# M3F-DETR 项目问题分析报告（第四版）

> 生成时间: 2026-07-28 | 修订依据: 全部 50+ 源码审查 + 三轮用户核验反馈

---

## 一、总览

| 编号 | 问题 | 严重程度 | 当前是否触发 |
|------|------|----------|:--:|
| #1 | Backbone → CMFA → FPN 通道配置脱节 | **高** | ✅ |
| #2 | CMFA 在高分辨率特征层做全局注意力，引发显存溢出（OOM） | **高** | ✅ |
| #3 | `inference.py` / `evaluate.py` 默认 backbone 与训练配置不一致 | **高** | 推理/评估时 |
| #4 | 代码注释中的硬编码通道值与实际逻辑不符 | **中** | ❌ |
| #5 | `datasets/` 模块未提交到仓库 | **中** | ❌ |
| #6 | `DINOTransformer.forward` 的 `query_embed` fallback 缺少防御 | **低** | ❌ |
| #7 | 代码整洁度问题（残留注释、空目录、不一致 collate_fn） | **低** | ❌ |

---

## 二、问题详情

### 问题 #1: Backbone → CMFA → FPN 通道配置脱节【高】

#### 现象

```
RuntimeError: Given groups=1, weight of size [256, 192, 1, 1],
expected input to have 192 channels, but got 48 channels instead.
```

错误位置: `models/neck/multimodal_fpn.py` 第 45-48 行 `lateral_convs[0]` 的 1×1 卷积。

#### 根因

**经过多轮迭代修改后，Backbone、CMFA、FPN 三者各自拥有不同的通道配置，彼此已经脱节**。

典型演进轨迹：
```
第一版: RGB=192, IR=96, Depth=96 → CMFA=dense → FPN=192
修改1:  将 CMFA 改为 96 通道
修改2:  Backbone 从 swin_large 换成 swin_small（P2 从 192→96）
修改3:  FPN 未同步更新，仍保持 192 通道的权重
结果:  每个模块的通道数全部不一致
```

`m3f_detr.py` 第 70 行从 backbone 读取通道传递给所有子模块：
```python
backbone_channels = self.rgb_backbone.channels
self.ir_encoder     = ThermalAdapter(backbone_channels[0])
self.depth_encoder  = DepthEncoder(backbone_channels[0])
self.fusion         = CMFA(dim=backbone_channels[0])
self.fpn            = MultiScaleFusion(in_channels_list=backbone_channels, ...)
```

理论上已统一，但服务器报错说明实际运行版本中**至少有一个模块的通道数来自硬编码而非 `backbone_channels`**。

> **注意**: timm 官方的 `features_only=True` 不会返回原始 RGB 图像；Swin 的 `WindowAttention` 内置 padding 机制，`img_size` 不整除 7 不会导致输出异常。这两项已排除。

#### 影响

- 任何 backbone 切换都会导致前向传播崩溃
- 持续"发现一个改一个"的修补模式会让配置越来越混乱
- 不根治则后续精度优化会被反复打断

#### 建议

**不推荐继续逐个位置修补通道值**。做一次彻底的接口统一：

**方案 A: 基于 `backbone_channels` 的动态适配**

1. **全局搜索硬编码通道数**（搜数字和搜函数调用，双管齐下）——

只搜数字 `48/96/192/384/768/1536` 可能漏掉 `channels[0]*2` 或 `hidden_dim//2` 这类表达式。建议同时搜索所有卷积/线性/注意力层的实例化位置，再人工逐行核查：

```bash
# 搜索数字
grep -rn "\b48\b\|\b96\b\|\b192\b\|\b384\b\|\b768\b\|\b1536\b" models/ --include="*.py"

# 搜索层实例化（防止硬编码藏在关键字参数中）
grep -rn "Conv2d(" models/
grep -rn "Linear(" models/
grep -rn "MultiheadAttention(" models/
```

2. **启动期 + 前向传播中打印所有模块的通道数和 shape**：

```python
# 启动期
print(f"[M3F-DETR] backbone={backbone_name}, channels={backbone_channels}")

# forward 中, 第 109 行之后:
print(f"  RGB feats: {[f.shape for f in rgb_features]}")
print(f"  IR feat:   {ir_feature.shape}")
print(f"  Depth feat: {depth_feature.shape}")
print(f"  Fused:     {fused.shape}")
```

调试完成后可移除或改为 `logging.debug`。

**方案 B: 集中式 ModelConfig（推荐用于长期维护）**

参考 MMDetection、Detectron2 模式，定义一个全局配置文件：

```python
# configs/model_config.py
class BackboneSpec:
    def __init__(self, channels, out_indices, patch_size, window_size,
                 embed_dim, depths, num_heads):
        self.channels = channels
        self.out_indices = out_indices
        self.patch_size = patch_size
        self.window_size = window_size
        self.embed_dim = embed_dim
        self.depths = depths
        self.num_heads = num_heads

BACKBONE_SPEC = {
    "swin_tiny":  BackboneSpec([96, 192, 384, 768],   [0,1,2,3], 4, 7, 96,  [2,2,6,2],   [3,6,12,24]),
    "swin_small": BackboneSpec([96, 192, 384, 768],   [0,1,2,3], 4, 7, 96,  [2,2,18,2],  [3,6,12,24]),
    "swin_base":  BackboneSpec([128, 256, 512, 1024], [0,1,2,3], 4, 7, 128, [2,2,18,2],  [4,8,16,32]),
    "swin_large": BackboneSpec([192, 384, 768, 1536], [0,1,2,3], 4, 7, 192, [2,2,18,2],  [6,12,24,48]),
}
```

这样的好处是：即使未来从 Swin 切换到 ConvNeXt 或 InternImage，接口依然统一。

**原则：项目中任何地方不再出现硬编码的通道数或结构参数。**

---

### 问题 #2: CMFA 在高分辨率特征层做全局注意力，引发显存溢出【高】

#### 现象

```
Tried to allocate 14.98 GiB of memory. Tried to allocate 16.36 GiB ...
```

训练/测试中频繁 OOM，即使显存 > 16GB 仍不够。

#### 根因

`models/m3f_detr.py` 第 121-125 行：CMFA 在 **P2 层**做三模态交叉注意力融合。

P2 层的空间分辨率最高（`H/4 × W/4`），假设输入 640×640：
```
P2:  160×160 = 25600 token
Attention: 25600² ≈ 6.55 亿对（交叉注意力还需额外拼接 IR+Depth → 51200 token）
```

`models/fusion/cmfa.py` 第 43-48 行将 IR + Depth 拼接（25600×2 = 51200 token），再与 RGB 做 `nn.MultiheadAttention`。注意力的显存消耗近似与 token 数的平方成正比。

若输入为 768×1280（比赛配置），P2 层 token 数更多，显存压力更大。

#### 影响

- 大分辨率训练直接 OOM，限制 batch size 和训练效率
- 通道匹配后仍会触发

#### 建议

**将 CMFA 从 P2 下移到 P3 层**（`H/8 × W/8`）：

```
P2 (H/4×W/4):  160×160 → 25600 token    → Attention ≈ 25600²
P3 (H/8×W/8):   80×80 →  6400 token    → Attention ≈  6400²

token 减少 4×, Attention 矩阵减少约 16×
```

```python
# m3f_detr.py 当前逻辑:
fused = self.fusion(
    rgb_features[0],       # P2 (H/4 × W/4)
    ir_feature,
    depth_feature,
)
fused_features = [fused] + list(rgb_features[1:])

# 建议改为 P3 融合:
ir_p3 = F.adaptive_avg_pool2d(ir_feature, rgb_features[1].shape[2:])
depth_p3 = F.adaptive_avg_pool2d(depth_feature, rgb_features[1].shape[2:])
fused = self.fusion(rgb_features[1], ir_p3, depth_p3)  # P3
fused_features = [rgb_features[0], fused] + list(rgb_features[2:])
```

> **⚠ 精度验证提醒**: P3 融合可显著降低显存，但是否影响 mAP **必须在本赛题数据集上做 A/B 实验验证**。对于 1920×1080 图像中 20×20、30×30 的小目标（行人、自行车等），P2 层的细节信息可能贡献较大，P3 层下采样后目标可能只剩数个像素。不要在实验前就假设"mAP 不下降"。

---

### 问题 #3: `inference.py` / `evaluate.py` Backbone 默认值与训练不匹配【高】

#### 现象

训练时使用 `swin_large`（配置于 `configs/m3f_dino.yaml` 和 `configs/train.yaml`），但 `inference.py` 和 `evaluate.py` 创建模型时未指定 `backbone_name`，默认使用 `swin_small`，导致 `load_state_dict` 失败。

#### 根因

**文件:** `inference.py` 第 165 行, `evaluate.py` 第 70 行

```python
# inference.py:165
model = M3F_DETR(num_classes=args.num_classes, ...)       # backbone 默认为 swin_small
# evaluate.py:70
model = M3F_DETR(num_classes=args.num_classes).to(device) # 同上
```

#### 影响

- 推理/评估时无法加载训练 checkpoint，架构不兼容导致 state_dict mismatch
- 使用者必须了解项目内部配置才能正确运行

#### 建议

**方案 A（推荐——最安全）:** 将模型配置存入 checkpoint，加载时自动恢复：

```python
# 训练保存时（utils/checkpoint.py）:
state = {
    "model": model.state_dict(),
    "cfg": {
        "backbone": backbone_name,
        "hidden_dim": hidden_dim,
        "num_queries": num_queries,
        "num_classes": num_classes,
    },
    "epoch": epoch,
}

# 推理加载时（inference.py）:
state = _safe_torch_load(args.checkpoint, map_location="cpu")
cfg = state.get("cfg", {})
model = M3F_DETR(
    num_classes=cfg.get("num_classes", 12),
    hidden_dim=cfg.get("hidden_dim", 256),
    num_queries=cfg.get("num_queries", 900),
    backbone_name=cfg.get("backbone", "swin_large"),  # 自动从 checkpoint 读取
    use_dn=False,
).to(device)
```

使用者只需 `python inference.py checkpoint.pth`，无需关心 backbone 类型。

**方案 B（快速修复）:** 添加 `--backbone` 参数，默认值改为 `swin_large`。

---

### 问题 #4: 代码注释中的硬编码通道值与实际逻辑不符【中】

#### 现象

`models/m3f_detr.py` 第 120-124 行注释写死 `192`；`models/neck/multimodal_fpn.py` 第 38-39 行默认值写死 `[96, 192, 384, 768]`。实际运行时由 `backbone_channels` 动态传入，不会触发默认值，但注释和默认值与动态适配的设计矛盾，容易误导维护者。

#### 建议

- 注释改为通用形式：`# P2 (B, ch[0], H/4, W/4)`
- `multimodal_fpn.py` 默认值改为 `None`，`forward` 中加断言要求非空

---

### 问题 #5: `datasets/` 模块未提交到仓库【中】

5 个文件引用该模块，但未纳入版本控制。**不是当前报错的直接原因**（test.py 已跑过 DataLoader），但会导致 `git clone` 后无法直接运行。建议提交到 Git。

---

### 问题 #6: `DINOTransformer.forward` 的 `query_embed` fallback 缺少防御【低】

`models/detector/transformer.py` 第 65-67 行在 `query_embed is None` 分支引用 `self.query_embed.weight`，但 `__init__` 未定义该属性。当前调用路径总是从 `dino_detector.py` 传入 query_embed，不会触发。属于防御性代码缺失，可在重构时顺手修复。

---

### 问题 #7: 代码整洁度问题【低】

| # | 位置 | 问题 |
|---|------|------|
| 7a | `engine/__init__.py:4` | 残留注释行 |
| 7b | `losses/` | 顶层目录仅含重导入的 `__init__.py`，真实实现在 `models/losses/` |
| 7c | `tools/test_dataloader.py:37` | `collate_fn=lambda x: x` 与标准 `collate_fn` 不一致 |

---

## 三、长期架构与算法建议

### 3.1 统一接口：集中式配置驱动

升级到 `BackboneSpec` 类（参考 MMDetection / Detectron2 模式），不仅包含通道数，还将 `out_indices`、`patch_size`、`window_size`、`embed_dim`、`depths`、`num_heads` 等结构参数统一管理，确保未来从 Swin 切换到 ConvNeXt、InternImage 等 backbone 后接口一致。

原则：**项目中任何地方不再出现硬编码的模型结构参数。**

### 3.2 融合位置探索

当前 CMFA 位于 Backbone 之后、FPN 之前：

```
Backbone → CMFA → FPN → DINO Head
```

近年 CVPR/ECCV 不少多模态检测工作将融合放在 Neck（FPN）之后或 Neck 中间层，利用 FPN 已融合多尺度信息再做跨模态交互。这在本项目后期精度优化阶段值得实验对比，可能比当前"早融合"方案取得更好的小目标召回。

### 3.3 深度模态编码改进

当前 `DepthEncoder`（`models/backbone/depth_encoder.py`）将深度图当作普通三通道图像用卷积处理。但深度图本质是几何信息（毫米级距离），与 RGB 的光度信息有根本差异，简单用 CNN 可能无法充分利用其特性。

后期冲榜时建议重点改进：

- **深度有效性掩码**: 深度为 0 或极小值视为无效区域（如远处天空），融合时屏蔽这些位置的注意力
- **几何感知编码**: 将深度离散化为距离分桶，按范围编码（如 `[0-5m, 5-10m, 10-20m, ...]`），减少极端值影响
- **归一化策略**: 按场景自适应归一化（如 max-depth normalization），而非全局固定值

这部分直接利用了本赛题三模态的独特优势，可能比继续堆 Attention 更有精度收益。

### 3.4 开发优先级路线图

当前问题已不是单个 Bug，而是**架构演进中接口不一致 + 显存瓶颈**。建议按以下顺序推进：

```
① 统一 Backbone → Fusion → FPN 通道接口
   保证任何 backbone 都能正常前向传播

② 增加调试日志和断言
   forward 中打印所有模块 shape，快速定位通道异常

③ 修复 checkpoint / backbone 配置管理
   cfg 存入 checkpoint，推理自动恢复

④ 验证 P2/P3 融合对精度和显存的影响
   在赛题数据集上做 A/B 实验，不假设 P3 不降精度

⑤ 重新设计 Depth 分支与融合策略
   深度有效性掩码、几何感知编码、归一化

⑥ 最后再做训练策略和超参数优化
   loss、augmentation、EMA 等，此时底层已稳定
```

核心原则：**先跑通，再省显存，最后刷精度。** 不要在没有稳定的前向传播上反复调试训练策略。

---

## 四、服务器 2026-07-29 运行结果与根因定位（修正版）

### 4.1 运行状态

```
Results: 1 passed, 3 failed
```

| 测试 | 状态 | 结论 |
|------|:--:|------|
| 环境检查 | ✅ | Python / PyTorch / timm 正常 |
| Test 1: DataLoader | ✅ | 数据集加载正常 |
| Test 2: Model Forward | ❌ | 前向传播崩溃（FPN 处） |
| Test 3: Loss Computation | ❌ | 同上（依赖 forward） |
| Test 4: Inference Pipeline | ❌ | 同上 |

### 4.2 启动日志

```
[M3F-DETR] backbone=swin_small, channels=[96,192,384,768],
           hidden_dim=256, num_queries=900
```

此值来自 `self.backbone.feature_info.channels()`。经服务器单独测试 timm 确认：timm 的 Swin 模型**元数据完全正确**，实际返回的通道数就是 [96, 192, 384, 768]。

### 4.3 报错原文

```
AssertionError: RGB 特征层 1 通道 48 ≠ 期望 192
  File "models/m3f_detr.py", line 122, in forward
    assert rgb_features[i].shape[1] == ch[i], (...)
```

### 4.4 根因：NHWC vs NCHW（最终确定）

服务器单独验证 timm Swin 的实际 forward 输出：

```python
m = timm.create_model("swin_small_patch4_window7_224", features_only=True)
# forward 输出格式:
#   0  torch.Size([1, 160, 160, 96])   ← NHWC!
#   1  torch.Size([1,  80,  80, 192])
#   2  torch.Size([1,  40,  40, 384])
#   3  torch.Size([1,  20,  20, 768])
```

**timm Swin 的 `features_only=True` 返回 NHWC 格式 `[B, H, W, C]`，而 M3F-DETR 所有下游模块（ThermalAdapter、DepthEncoder、CMFA、FPN、DINO Head）均按 PyTorch 标准 CNN 格式 `[B, C, H, W]` 设计。**

`rgb_backbone.py` 的 `forward()` 直接将 timm 输出透传，未做 permute：

```python
def forward(self, x):
    feats = self.backbone(x)   # 返回 [B,H,W,C]
    return feats               # 直接透传 → 后续模块用 shape[1] 读 H 而非 C
```

这导致所有下游对整个 tensor 的维度解读完全错位：

| 层 | timm 实际输出 | 代码按 NCHW 误解读 | 实际被当作 C 的维度 |
|----|-------------|-------------------|-------------------|
| P2 | `[1, 160, 160, 96]` | `[1, 160, 160]` C=160? | 空间 H=160 |
| P3 | `[1, 80, 80, 192]` | `[1, 80, 80]` C=80? | 空间 H=80 |
| P4 | `[1, 40, 40, 384]` | `[1, 40, 40]` C=40? | 空间 H=40 |

这解释了"48 通道"的前世今生：测试图像尺寸变化导致误读的 H 值在 48 附近（例如输入高度 192→P3 的 H=48→被当作 C）。

**timm `feature_info.channels()` 自始至终是正确的。`self.backbone.channels = [96, 192, 384, 768]` 也是正确的。真正的 bug 是缺少一行 `permute(0,3,1,2)`。**

### 4.5 排除项

| 排除 | 原因 |
|------|------|
| IR/Depth 编码器 | forward 在 backbone 后就中断，从未进入 IR/Depth |
| CMFA `dim//2` 降维 | CMFA 从未被调用 |
| FPN 通道配置 | FPN 从未被调用 |
| timm 版本 bug | timm Swin NHWC 是其正常行为 |
| `feature_info.channels()` 错误 | 元数据正确，只是格式不一致 |

### 4.6 修复方案

**文件:** `models/backbone/rgb_backbone.py`

在 `forward()` 中添加 NHWC → NCHW 转换：

```python
def forward(self, x):
    feats = self.backbone(x)
    out = []
    for f in feats:
        if f.dim() == 4:
            f = f.permute(0, 3, 1, 2).contiguous()  # [B,H,W,C] → [B,C,H,W]
        out.append(f)
    return out
```

修复后服务器验证，正确输出应为：
```
0 torch.Size([1, 96, 160, 160])    ← [B,C,H,W]
1 torch.Size([1, 192, 80, 80])
2 torch.Size([1, 384, 40, 40])
3 torch.Size([1, 768, 20, 20])
```

### 4.7 CMFA OOM（仍待 backbone 修复后验证）

CMFA P2 层全局 MultiheadAttention 的 OOM 风险在 backbone NHWC 问题修复后会自然暴露，因为在 NHWC 状态下特征维度被误读、流程提前崩溃，所以先前看不到 OOM。修复后预测 Test 1-2 应会通过，Test 3-4 才进入 CMFA 注意力计算。

v0.3.0 实现的 `use_downsample` 模式（2×2 pool）已就绪，修复 backbone 后即可验证。token 从 129600→32400，attention 矩阵降 ~16×。

### 4.8 阶段性结论（最终）

| 判断 | 状态 |
|------|:--:|
| timm `feature_info.channels()` | ✅ 正确 [96, 192, 384, 768] |
| feature forward 输出通道 | ✅ 正确 [96, 192, 384, 768] |
| **tensor 内存格式** | **❌ NHWC 未转 NCHW — 根因** |
| IR/Depth 编码器 | ✅ 无关 |
| CMFA | ✅ 无关 |
| FPN | ✅ 无关 |
| v0.3.0 断言定位价值 | ✅ 精确定位了故障位置（虽原因推断有偏差） |
| CMFA OOM 风险 | ⚠️ backbone 修复后暴露，use_downsample 已就绪 |

---

## 五、修复行动清单（最终版）

根因已确定：**timm Swin NHWC → NCHW 缺失一行 permute**。修复只需一步，后续按序推进。

```
① 修 backbone NHWC → NCHW 转换
   └── models/backbone/rgb_backbone.py forward():
       for f in feats: f = f.permute(0,3,1,2).contiguous()

② 跑 test.py 验证
   └── 预期 Test 1-2 通过, Test 3-4 进入 CMFA 计算

③ CMFA 显存优化
   └── v0.3.0 use_downsample 已实现，验证 OOM 是否消失

④ 精度优化
   └── loss / augmentation / EMA / 训练策略
```

**核心原则**：backbone NHWC 转 NCHW 是整个管线的底层假设。这一行修复后，前两个月所有"通道不一致"的症状全部自然消失。

**核心原则**：backbone 不正确则下游一切皆空。先修 backbone，再谈 CMFA、显存和精度。

---

## 六、服务器 2026-07-30 第二轮问题（v0.3.2 已解决 backbone）

v0.3.2 修复 backbone NHWC→NCHW 后，`Results: 1 passed, 3 failed` 的失败位置从 FPN 迁移到了 Transformer。当前不再有"通道不一致"问题，已进入 DINO-DETR 接口对齐阶段。

### 6.1 运行状态（最新）

```
Results: 1 passed, 3 failed
```

| 测试 | 状态 | 原因 |
|------|:--:|------|
| Test 1: DataLoader | ✅ | 正常 |
| Test 2: Model Forward | ❌ | Transformer hidden_dim 不匹配（新增 #1） |
| Test 3: Loss Computation | ❌ | attn_mask 未初始化（新增 #2） |
| Test 4: Inference Pipeline | ❌ | 同 #2 |

### 6.2 新增问题 #1：Transformer hidden_dim 与位置编码维度不匹配

**报错原文:**
```
RuntimeError: The size of tensor a (256) must match the size of tensor b (512)
at non-singleton dimension 2
```

**调用链:**
```
m3f_detr.py:154 → dino_detector.py:150 → transformer.py:62
  memory = memory + pos
```

**根因:**
```
memory.shape[-1] = 256   ← hidden_dim=256
pos.shape[-1]    = 512   ← 2 * num_pos_feats，num_pos_feats=256 时为 512
```

`PositionEmbeddingSine` 的 `num_pos_feats` 在构造时若被设为 `hidden_dim` 同一个值（如 256），输出维度会变成 `2 * 256 = 512`，与 hidden_dim=256 的 memory 维度不匹配。

DETR 的设计语义是：`PositionEmbeddingSine(num_pos_feats=hidden_dim // 2)`，这样 `2 * num_pos_feats == hidden_dim`，可与 memory 直接相加。

**修复:**
1. 搜索：`grep -rn "num_pos_feats" models/`
2. 将 `num_pos_feats=256` 改为 `num_pos_feats=hidden_dim // 2`（或直接传入 `hidden_dim // 2` 作为参数）
3. 涉及文件：`models/detector/position_encoding.py`、`models/detector/dino_detector.py`

### 6.3 新增问题 #2：`attn_mask` 未初始化

**报错原文:**
```
UnboundLocalError: cannot access local variable 'attn_mask' where it is not associated with a value
```

**调用链:**
```
models/detector/dino_detector.py:155 → 使用 attn_mask 时未定义
```

**根因:**
当前代码大致结构：
```python
if self.use_dn and self.training and targets is not None:
    ...
    attn_mask = ...  # 仅 DN 训练时赋值
# 后续代码无条件使用 attn_mask
hs = self.transformer(tgt, src, pos, query_embed, mask=attn_mask)
```

当 `use_dn=False`（测试默认）或推理时，`attn_mask` 从未被赋值，引用时触发 `UnboundLocalError`。

**修复:**
在 `dino_detector.py` 的 Transformer 调用前显式初始化：

```python
attn_mask = None
if self.use_dn and self.training and targets is not None:
    query_embed, targets, attn_mask, dn_meta = prepare_for_dn(...)
else:
    dn_meta = None
```

保证 `attn_mask=None` 始终有定义，传给 transformer 的 mask 参数；transformer 内部也需要相应处理 `mask=None` 的分支（已存在），无需修改。

### 6.4 阶段性结论

| 组件 | 状态 |
|------|:--:|
| Swin Backbone NHWC→NCHW | ✅ 已修复 (v0.3.2) |
| 通道配置脱节 | ✅ 已修复 |
| RGB 多尺度特征对齐 | ✅ 正确 |
| IR/Depth/CMFA/FPN | ✅ 通过 v0.3.2 验证（未在前向崩溃中触发） |
| **Transformer hidden_dim 对齐** | **❌ #1 待修** |
| **attn_mask 初始化** | **❌ #2 待修** |
| Transformer 主体逻辑 | ✅ 待 #1/#2 修完后验证 |

### 6.5 新优先级清单

```
① 修 PositionEmbeddingSine 的 num_pos_feats（256 → 128 或改为 hidden_dim//2）
② 修 dino_detector.py 中 attn_mask 的初始化（保证有 None 默认值）
③ 跑 test.py 验证 Test 2 通过
④ 验证 CMFA OOM 是否被 v0.3.0 use_downsample 解决
⑤ 精度优化
```

---

## 七、服务器 2026-07-31 第三轮问题（v0.3.3 已解决 Transformer）

### 7.1 运行状态（最新）

```
Results: 3 passed, 1 failed out of 4
```

| 测试 | 状态 | 备注 |
|------|:--:|------|
| Test 1: DataLoader | ✅ | Dataset size: 2000, RGB/IR/Depth shape (1,3,384,640) |
| Test 2: Model Forward | ✅ | backbone=swin_small, channels=[96,192,384,768]; pred_logits=(1,900,13), pred_boxes=(1,900,4) |
| Test 3: Loss Computation | ❌ | `'float' object has no attribute 'item'` |
| Test 4: Inference Pipeline | ✅ | Detections (conf > 0.3): 0 boxes（数据集含真实框 2 个，无检测结果为预期阈值问题） |

**关键进展**: Test 1/2/4 已全部通过，仅剩 Test 3 Loss 内部调用 bug。前向传播链路完整跑通，已具备训练前条件。

### 7.2 新增问题 #3：Loss 输出 `.item()` 二次调用

**完整报错信息:**
```
Test 'Loss Computation' FAILED: 'float' object has no attribute 'item'

Traceback (most recent call last):
  File "/root/autodl-tmp/aic_race/M3F-DETR/test.py", line 201, in main
    if test_fn(data_root):
  File "/root/autodl-tmp/aic_race/M3F-DETR/test.py", line 124, in test_loss_computation
    print(f"  {k}: {v.item():.4f}")

AttributeError: 'float' object has no attribute 'item'
```

**报错发生时间:** 2026-07-31 13:53（服务器 `python test.py` 运行 Test 3 时触发）
**触发条件:** `test_loss_computation()` 调用 `criterion(output, targets)` 后遍历 `loss_dict.items()` 并打印
**影响范围:** 仅 Test 3 Loss Computation 失败，其他 3 个测试全部通过；不阻塞推理（Test 4 通过）和前向（Test 2 通过）
**严重程度:** 中（最后一次回归，修复即可让所有测试通过）

**根因:**
`DINOLoss.forward()` 返回的字典中，前三个 loss 已被调用 `.item()` 转 float，第四个 `loss` 仍为 tensor：

```python
# models/losses/dino_loss.py:131-136
return {
    "loss_class": loss_class.item(),   # float
    "loss_bbox":  loss_bbox.item(),    # float
    "loss_giou":  loss_giou.item(),    # float
    "loss":       total,                # tensor (未 .item())
}
```

而 `test.py:124` 对**所有**值调用 `v.item()`：
```python
for k, v in loss_dict.items():
    print(f"  {k}: {v.item():.4f}")
```

对 float 调用 `.item()` 触发 `AttributeError`。

**修复（两种方案任选其一）:**

**方案 A — Loss 返回 tensor（推荐）:**
```python
# models/losses/dino_loss.py
return {
    "loss_class": loss_class,        # tensor
    "loss_bbox":  loss_bbox,         # tensor
    "loss_giou":  loss_giou,         # tensor
    "loss":       total,              # tensor
}
```

调用方按需 `.item()`，例如 `test.py` 打印时统一用 `v.item() if torch.is_tensor(v) else v`。

**方案 B — Test 用 `.detach().item()` 兼容:**
```python
# test.py:124
for k, v in loss_dict.items():
    val = v.item() if torch.is_tensor(v) else v
    print(f"  {k}: {val:.4f}")
```

方案 A 更彻底修复问题源头；方案 B 仅作测试端兼容。

### 7.3 阶段性结论

| 组件 | 状态 |
|------|:--:|
| Backbone (NHWC→NCHW) | ✅ v0.3.2 |
| 通道配置脱节 | ✅ v0.3.0/v0.3.1 |
| Transformer hidden_dim 对齐 | ✅ v0.3.3 (PositionEmbeddingSine 修正) |
| `attn_mask` 初始化 | ✅ v0.3.3 |
| **Loss `.item()` 二次调用** | **❌ 最后一个 bug** |
| CMFA OOM | ⚠️ v0.3.0 `use_downsample` 已实现但本轮测试未触发 OOM（loss 在 forward 之后崩溃） |

### 7.4 优先级（收尾）

```
① 修 dino_loss.py 返回 tensor 而非 float（方案 A）
② 跑 test.py 验证 Test 3 通过 → 4/4 通过
③ 训练脚本 smoke test（少量数据 + 少量 epoch）
④ 准备 AIC2026 比赛训练
```

---

## 八、服务器 2026-08-01 训练启动报错（train.py optimizer）

### 8.1 运行状态

```
$ python train.py --config configs/debug.yaml

[1] Loading dataset...    OK (Total samples: 2000)
[2] Building model...     OK (backbone=swin_tiny, channels=[96,192,384,768], hidden_dim=128, num_queries=100)
[3] Training stage trainer: 32.6M
[4] (混合精度初始化)
[5] Training starts!      FAIL
```

### 8.2 完整报错信息

```
Traceback (most recent call last):
  File "/root/autodl-tmp/aic_race/M3F-DETR/train.py", line 390, in <module>
    main()
  File "/root/autodl-tmp/aic_race/M3F-DETR/train.py", line 316, in main
    optimizer = build_optimizer(model, config)
  File "/root/autodl-tmp/aic_race/M3F-DETR/train.py", line 101, in build_optimizer
    backbone_lr = config["optimizer"].get("backbone_lr", config["optimizer"]["lr"] * 0.1)

TypeError: can't multiply sequence by non-int of type 'float'
```

### 8.3 报错发生时间与触发条件

- **时间:** 2026-08-01 09:07（服务器启动训练）
- **触发条件:** `python train.py --config configs/debug.yaml` 启动后，到 `build_optimizer(model, config)` 调用
- **位置:** `train.py:101`，`build_optimizer` 中 backbone_lr 计算行

### 8.4 报错影响范围与严重程度

- **影响范围:** 训练完全无法启动；推理、评估、测试脚本均不受影响
- **严重程度:** **高**（阻塞训练启动，修复后即可进入实际训练阶段）

### 8.5 报错根本原因分析

**关键观察 1:** YAML 配置文件 `configs/debug.yaml` 内容正确：

```yaml
optimizer:
  name: AdamW
  lr: 0.0001
  backbone_lr: 0.00001
  weight_decay: 0.05
```

**关键观察 2:** 服务器验证表明 `yaml.safe_load` 完全正常：

```python
>>> type(cfg["optimizer"]["lr"])
<class 'float'>
```

**关键观察 3:** 服务器端 `train.py` 配置加载也是用 `yaml.safe_load`（第 129 行）：

```python
config = yaml.safe_load(f)
```

**关键观察 4:** Python 中错误 `can't multiply sequence by non-int of type 'float'` 唯一触发条件是 `config["optimizer"]["lr"]` 在执行 `* 0.1` 时**实际值是字符串**。

---

### 8.5.1 关键发现：根因 100% 确认（PyYAML 把 `1e-4` 解析为字符串）

服务器实测验证：

```python
>>> cfg = yaml.safe_load(open("configs/debug.yaml"))
>>> print(cfg["train"]["stages"][0]["lr"])
1e-4
>>> print(type(cfg["train"]["stages"][0]["lr"]))
<class 'str'>
```

**不是** `train.py`，**不是** `build_optimizer`，**不是**代码污染了类型。是 **PyYAML 在此环境下把 `1e-4` 解析为了字符串**。

### 8.5.2 根因机制

YAML 1.1 规范对科学计数法的处理存在版本差异：

| YAML 写法 | PyYAML 6.x 标准行为 | 项目实际行为 |
|----------|-------------------|------------|
| `lr: 0.0001` | float `(0.0001)` | float ✓ |
| `lr: 1e-4` | float `(1e-4)` | **`str` ('1e-4')** ❌ |

虽然 PyYAML 6.0.3 文档声称 `1e-4` 应被解析为 float，但部分 Linux 容器环境（特别是基于 PyYAML + 严格 YAML 1.2 解释器的版本）会将其视为字符串。这与 PyYAML 实际版本、libyaml 绑定、Linux 区域设置有关。

调用链路：

```
configs/debug.yaml:28
  lr: 1e-4
       ↓ yaml.safe_load  (项目环境)
{"lr": "1e-4"}  ← 字符串！
       ↓ train.py:315 赋值
config["optimizer"]["lr"] = stage["lr"]  ← 仍是字符串
       ↓ build_optimizer:101
"1e-4" * 0.1  →  TypeError: can't multiply sequence by non-int of type 'float'
```

### 8.5.3 可能的隐藏字符污染

在与同事合作时，YAML 文件可能通过复制粘贴流入不可见字符。可通过 `cat -A` 检查：

```bash
cat -A configs/debug.yaml | sed -n '24,32p'
```

若 `1e-4` 中间出现 `M-bM-^@M-^` (UTF-8 BOM) 或 `M-bM-^TM-^` (宽字符) 等异常字符，PyYAML 会降级为字符串。即便字符正常，`1e-4` 在某些环境下仍会被默认为字符串。

### 8.6 修复方案

#### 方案 A — 立即可用（修改 YAML 即可）

将 `configs/debug.yaml` 第 28 行：

```yaml
# 错误写法
lr: 1e-4

# 正确写法
lr: 0.0001
```

**优点：** 1 行改动，零代码风险
**缺点：** 治标不治本——日后任何人写 `lr: 1e-3` 仍会触发

#### 方案 B — 健壮性修复（推荐）

两处代码改动：

**修改 1：** `train.py:314-315`

```python
if stage.get("lr") is not None:
    config["optimizer"]["lr"] = float(stage["lr"])
```

**修改 2：** `build_optimizer()` 入口加 `float()` 守卫

```python
lr = float(config["optimizer"]["lr"])
backbone_lr = float(config["optimizer"].get("backbone_lr", lr * 0.1))
weight_decay = float(config["optimizer"].get("weight_decay", 0.05))
```

**优点：** 对任何 YAML 写法（`1e-4`、`"1e-4"`、普通浮点）都有容错能力
**缺点：** 增加 3 行代码

#### 方案 C — 全面防护

在 `train.py` 加载配置后做全局 `float()` 转换：

```python
def _safe_float(d):
    for k, v in d.items():
        if isinstance(v, str):
            try:
                d[k] = float(v)
            except ValueError:
                pass
        elif isinstance(v, dict):
            _safe_float(v)
```

**优点：** 一劳永逸
**缺点：** 改动较大

### 8.7 阶段性结论

| 组件 | 状态 |
|------|:--:|
| Test 1-4 全部通过 | ✅ v0.3.4 |
| `yaml.safe_load`（项目环境） | ⚠️ `1e-4` 被解析为 str |
| `yaml.safe_load`（官方行为） | ✅ 应为 float |
| **build_optimizer 字符串污染** | ✅ v0.3.5 已修复 |
| 训练启动 | ✅ v0.3.5 |

---

## 九、错误状态总览（2026-08-01 16:51 快照）

### 9.1 已解决错误清单

| 编号 | 标题 | 解决版本 | 解决日期 |
|------|------|---------|---------|
| #1 | 多模态网络通道配置漂移 | v0.2.0 | 2026-07-28 |
| #2 | timm Swin NHWC/NCHW 格式不一致 | v0.3.2 | 2026-07-30 |
| #3 | PositionEmbeddingSine 实现偏差（512 vs 256） | v0.3.3 | 2026-07-31 |
| #4 | `attn_mask` 未初始化 | v0.3.3 | 2026-07-31 |
| #5 | Loss `.item()` 二次调用 | v0.3.4 | 2026-07-31 |
| #6 | train.py:101 TypeError（PyYAML 解析 `1e-4` 为字符串） | v0.3.5 | 2026-08-01 |
| #7 | CMFA OOM at P2 + 接口校验 | v0.3.0 | 2026-07-28 |

### 9.2 当前错误（2026-08-01 16:51 实测）

#### 错误 #8: evaluate.py mAP=0（无预测框）

- **编号:** #8
- **标题:** 模型推理后预测框数为 0，mAP 全 0
- **状态:** 🔴 进行中
- **严重程度:** 中（影响评估但不影响推理链路）
- **最后更新:** 2026-08-01 16:51

**现象:**
```
[3] 开始推理...
加载数据：2000
加载模型：√ (backbone=swin_tiny, channels=[96,192,384,768])
使用普通权重
预测框：0
真实框：14417
⚠ 无预测或无标注，跳过 mAP 计算

mAP50-95: 0.0000
mAP50:     0.0000
mAP75:     0.0000
```

**复现步骤:**
1. `python evaluate.py --checkpoint checkpoints/debug/final.pth --data-root data/train`
2. 模型初始化、数据加载、checkpoint 加载均正常
3. `engine/evaluator.py:42` 出现 `FutureWarning` 后继续执行
4. 推理结束后得到 0 个预测框 vs 14417 个真实框

**预期行为:** 模型应输出大量预测框，经置信度阈值过滤（默认 0.3）后保留部分有效预测，mAP 非 0。

**根因分析（按概率）:**
1. **训练轮数不足（最可能）：** debug 配置仅 5 个 epoch，模型未充分收敛
2. **置信度阈值过高：** 默认 0.3 偏高，可尝试 `--conf-threshold 0.05`
3. **checkpoint 加载配置差异：** checkpoint 中含 `dn_query_embed`，evaluate 用 `strict=False` 跳过；可能不影响推理但需注意
4. **use_dn 默认值不一致（次要）：** 训练使用 `use_dn=True` 默认值（保存了 dn_query_embed），评估和推理也可能使用同样默认值但缺少配置读取

**建议排查顺序:**
1. 用更低置信度重跑：`python evaluate.py --conf-threshold 0.05 ...`
2. 检查训练日志 loss 曲线是否在下降
3. 增大训练 epoch 数（debug 5→full 100+）
4. 确认训练和评估使用了相同的 backbone 配置

---

#### 错误 #9: inference.py state_dict 加载失败

- **编号:** #9
- **标题:** `inference.py` 加载 checkpoint 时检测到 `detector.dn_query_embed.weight` 不属于模型参数
- **状态:** 🔴 进行中
- **严重程度:** **高**（阻塞推理流水线生成 submission）
- **最后更新:** 2026-08-01 17:18

**完整报错信息:**
```
RuntimeError: Error(s) in loading state_dict for M3F_DETR:
  Unexpected key(s) in state_dict: "detector.dn_query_embed.weight".
```

**调用链:**
```
inference.py:197 → torch.load → model.load_state_dict (默认 strict=True)
```

**根因（2026-08-01 17:18 100% 确认）:**

实测 `grep -R "use_dn" -n .` 输出：

| 文件 | 行号 | use_dn 值 |
|------|------|----------|
| `models/detector/dino_detector.py` | 47 | `use_dn=True` (默认值) |
| `models/m3f_detr.py` | 58 | `use_dn=True` (默认值) |
| `test.py` | 72 | `use_dn=True` (Model Forward 测试) |
| test.py | 107 | `use_dn=False` |
| test.py | 152 | `use_dn=False` |
| **inference.py** | **185** | **`use_dn=False, # 推理时不需要 DN`** |
| test-checkpoint.py | 72 | `use_dn=True` |
| test-checkpoint.py | 107, 151 | `use_dn=False` |

**关键不一致:** `inference.py:185` 显式 `use_dn=False`，但训练时 `M3F_DETR` 使用默认 `use_dn=True`，导致训练出来的 checkpoint 包含 `dn_query_embed.weight`，而推理创建的模型无此参数。

**实测 `grep -n "load_state_dict" evaluate.py inference.py` 输出:**

```
evaluate.py:106   model.load_state_dict(state["ema"])
evaluate.py:110   model.load_state_dict(state["model"])
evaluate.py:113   model.load_state_dict(state_to_load)
inference.py:195  model.load_state_dict(state["ema"])
inference.py:197  model.load_state_dict(state["model"])
inference.py:201  model.load_state_dict(state_to_load)
```

**所有调用均无 `strict=False`**，均使用默认 `strict=True`。这意味着：
- `evaluate.py` 之所以能加载成功，是因为 evaluate 走的是 `state["ema"]` 分支（无 DN 参数）
- `inference.py` 走 `state["model"]` 分支（含 `dn_query_embed`），与 `use_dn=False` 模型结构冲突

**根因诊断:**
1. 训练用 `use_dn=True`（默认），保存 `dn_query_embed.weight` 到 checkpoint
2. `evaluate.py` 默认加载 `ema` 分支（不含 DN），所以 strict=True 也不报错
3. `inference.py` 默认加载 `model` 分支（含 DN），但创建模型时 `use_dn=False`，strict=True → RuntimeError

**修复方案（待用户确认后实施）:**

方案 A — 最小修复（推荐）: 在 `inference.py` 改 `use_dn=True` 以匹配训练：

```python
# inference.py:185
model = M3F_DETR(..., use_dn=True).to(device)  # 与训练保持一致
```

方案 B — 改 `strict=False`（容忍 DN 参数）:

```python
# inference.py:197
model.load_state_dict(state["model"], strict=False)
```

但 DN 参数会在推理时白白占用显存且无用，不如方案 A 干净。

方案 C — 训练改 `use_dn=False`：

需要在 `train.py` 创建模型时显式 `use_dn=False`，并删除 `dino_detector.py` 中相关 DN 分支代码。改动较大，不推荐。

**推荐方案 A**：1 行改动，与训练结构完全对齐；方案 B 仅作兜底。

---

#### 错误 #10: AMP API FutureWarning

- **编号:** #10
- **标题:** `torch.cuda.amp.autocast` 已废弃，新 API 为 `torch.amp.autocast('cuda', ...)`
- **状态:** 🟡 低优（不阻塞但有警告）
- **严重程度:** 低（不影响功能，仅污染日志）
- **最后更新:** 2026-08-01 16:51

**现象:**
```
engine/evaluator.py:42 FutureWarning: `torch.cuda.amp.autocast(args...)` is deprecated.
Please use `torch.amp.autocast("cuda", args...)` instead.
```

**修复方案:**
```python
# 旧代码
with torch.cuda.amp.autocast(enabled=use_amp, dtype=torch.bfloat16):
    ...

# 新代码
with torch.amp.autocast("cuda", enabled=use_amp, dtype=torch.bfloat16):
    ...
```

涉及文件: `engine/evaluator.py`、`utils/amp.py`（如有独立封装）

---

### 9.3 项目完成度总览（2026-08-01 16:51）

| 模块 | 状态 |
|------|:--:|
| Dataset | ✅ |
| Dataloader | ✅ |
| 三模态读取 | ✅ |
| Model Forward | ✅ |
| Loss | ✅ |
| Backward | ✅ |
| Optimizer | ✅ |
| AMP | ✅（仅 FutureWarning 待清理） |
| Scheduler | ✅ |
| Checkpoint 保存 | ✅ |
| Checkpoint 加载 | ✅ (evaluate) / ❌ (inference) |
| Evaluation | ⚠️ mAP=0（无预测） |
| Inference | ❌ strict=True 导致加载失败 |
| Submission 生成 | ⏳ 未验证 |

### 9.4 优先级清单（剩余问题）

```
高优先级:
  ① inference.py: strict=False 加载（错误 #9）
  ② 模型预测框数为 0（错误 #8，需训练更多 epoch 或调阈值）

低优先级:
  ③ AMP API 更新（错误 #10）
  ④ evaluate 与 inference 配置文件统一（#8 副作用）
```

### 9.5 紧急建议（不修改代码）

如需立即调试：
1. `grep -n "load_state_dict" evaluate.py inference.py` 确认 strict 参数差异
2. `grep -R "use_dn" -n .` 确认训练/推理 use_dn 一致性
3. `python evaluate.py --conf-threshold 0.05 ...` 验证置信度阈值影响
4. 完整训练（≥50 epoch）后重新评估

---

## 十、2026-08-02 新报错：v0.3.5 修复后 TypeError 复发

### 10.1 运行状态与报错

```
$ python train.py --config configs/m3f_dino.yaml

  Batch size: 4 (per GPU)          ← 注意：v0.4.0 已改为 8！
[M3F-DETR] backbone=swin_large, channels=[192,384,768,1536], hidden_dim=256, num_queries=900
  Total params: 209.1M

[3] Initializing EMA...
[5] Training starts!
  Stage: freeze_backbone | Epochs: 0→50

Traceback (most recent call last):
  File ".../train.py", line 390, in <module>
    main()
  File ".../train.py", line 316, in main
    optimizer = build_optimizer(model, config)
  File ".../train.py", line 101, in build_optimizer
    backbone_lr = config["optimizer"].get("backbone_lr", config["optimizer"]["lr"] * 0.1)
TypeError: can't multiply sequence by non-int of type 'float'
```

- **编号:** #11
- **标题:** v0.3.5 修复后 TypeError 复发（根因：服务器代码未同步）
- **状态:** 🔴 进行中（非代码 bug，是部署问题）
- **严重程度:** 高（阻塞正式训练）
- **最后更新:** 2026-08-02 10:13

### 10.2 关键证据：这不是新的代码 bug，是服务器未同步

**证据 1 — 报错行号是旧代码：**

| 代码位置 | 服务器实际内容（报错栈） | 本地 v0.3.5+ 内容 |
|---------|------------------------|------------------|
| train.py:101 | `backbone_lr = config["optimizer"].get("backbone_lr", config["optimizer"]["lr"] * 0.1)` | `lr = float(config["optimizer"]["lr"])`（float 守卫） |

服务器栈中的 `train.py:101` 仍是 v0.3.5 **修复前**的原始代码；本地该行已经是 `float()` 守卫。**结论：服务器 train.py 未同步 v0.3.5 的修改。**

**证据 2 — Batch size 与 v0.4.0 不一致：**

```
服务器: Batch size: 4 (per GPU)
本地 v0.4.0: configs/m3f_dino.yaml batch_size: 8
```

**证据 3 — 触发路径（与 #6 完全相同的链路）：**

```
configs/m3f_dino.yaml:34   lr: 1e-4   (train.stages[0].lr，科学计数法)
configs/m3f_dino.yaml:38   lr: 5e-5   (train.stages[1].lr)
         ↓ 服务器 PyYAML 解析（同 #6 的已知行为）
"1e-4" (字符串)
         ↓ 旧代码 train.py:315 无 float() 守卫
config["optimizer"]["lr"] = "1e-4"
         ↓ 旧代码 train.py:101
"1e-4" * 0.1 → TypeError
```

**核心结论：** 服务器上的代码停留在 v0.3.5 之前（甚至 v0.3.4），本地已完成的 v0.3.5（float 守卫）、v0.4.0（batch 8 + BF16 + compile）、v0.4.1（use_dn 对齐）均未同步到服务器。

### 10.3 影响范围与严重程度

- **影响:** 正式训练（m3f_dino.yaml，swin_large 209M）无法启动
- **与 #6 的关系:** 同一根因（PyYAML 解析科学计数法）再次触发，但这次是因为**部署不同步**而非代码缺失
- **severity:** 高 — 阻塞训练，但**不是代码问题**，同步代码即可解决

### 10.4 建议操作（不改代码）

```
① 在服务器执行 git log --oneline -5   # 确认服务器 HEAD 位置
② 在服务器执行 git pull origin main   # 同步到 v0.4.1
③ 确认服务器 train.py:101 已是 float() 守卫
④ 重新运行 python train.py --config configs/m3f_dino.yaml
```

**预期:** 同步到 v0.4.1 后，`float()` 守卫（v0.3.5）将免疫所有科学计数法字符串，TypeError 不再出现。

### 10.5 阶段结论

| 项 | 状态 |
|----|:--:|
| 本地代码（v0.4.1） | ✅ 含 float() 守卫，无此 bug |
| 服务器代码 | ❌ 停留在 v0.3.5 之前（train.py:100 无守卫） |
| TypeError 复发原因 | **服务器未 git pull**，非新代码缺陷 |
| 建议 | 同步代码后重跑 |

### 10.6 二次复发确认（2026-08-04 19:20）

服务器再次触发完全相同的报错：

```
$ python train.py --config configs/m3f_dino.yaml
...
Stage: freeze_backbone | Epochs: 0→50

Traceback (most recent call last):
  File ".../train.py", line 389, in <module>
    main()
  File ".../train.py", line 315, in main
    optimizer = build_optimizer(model, config)
  File ".../train.py", line 100, in build_optimizer
    backbone_lr = config["optimizer"].get("backbone_lr", config["optimizer"]["lr"] * 0.1)
TypeError: can't multiply sequence by non-int of type 'float'
```

| 项 | 状态 |
|----|:--:|
| 容器 ID | `wegjcc9jvq-ae673915`（与 2026-08-02 同一容器） |
| `Batch size: 4 (per GPU)` | ⚠️ 未变（v0.4.0 已改 8） |
| `utils/amp.py:24 FutureWarning` | ⚠️ 未变（v0.4.0 应消） |
| `train.py:100` 代码 | ⚠️ 仍是 `backbone_lr = ... * 0.1`（无 float 守卫） |
| **结论** | **服务器 `git pull` 未生效或未执行** |

**注：** 本地 5 个 commit 已于 2026-08-02 通过系统代理 `127.0.0.1:7877` 推送到 origin/main（`c40b6fa..dc2052f`），但服务器端代码未更新。可能是：

1. 服务器上 `git pull` 因冲突被拒绝
2. 服务器 `git pull` 执行成功但未实际生效（cache 或 detached HEAD）
3. 服务器上某个 branch 未与 origin/main 同步

**建议排查步骤：**

```bash
cd ~/autodl-tmp/aic_race/M3F-DETR
git status                    # 确认是否干净
git log --oneline -3          # 确认 HEAD 是否为 dc2052f
git fetch origin              # 显式 fetch
git reset --hard origin/main  # 强制对齐到最新远程分支
sed -n '99,108p' train.py     # 确认 float() 守卫已落地
python train.py --config configs/m3f_dino.yaml
```

预期 `train.py:101` 应为 `lr = float(config["optimizer"]["lr"])`，batch size 显示 8，AMP BF16 生效。

### 10.7 错误编号状态汇总（2026-08-06 08:55 更新）

| 编号 | 标题 | 状态 |
|------|------|:--:|
| #1-#7 | 通道漂移/NHWC/位置编码/attn_mask/Loss/YAML | ✅ 全部已解决 |
| #8 | evaluate.py mAP=0（debug 5 epoch 不充分） | 🔴 进行中（需正式训练） |
| #9 | inference.py use_dn 不对齐 | ✅ v0.4.1 已解决（2026-08-06 实测确认） |
| #10 | AMP API FutureWarning | 🟡 低优待清理 |
| **#11** | **TypeError 复发（服务器未同步）** | **✅ 已解决（代码同步后消失）** |

### 10.8 里程碑确认：推理全流程跑通（2026-08-06 08:55）

服务器执行 `python inference.py --checkpoint checkpoints/debug/final.pth` 输出：

```
[2] Loading model...
Backbone: swin_tiny (from checkpoint cfg)     ← checkpoint 配置自动恢复 ✓
✓ 模型加载成功                                ← use_dn=True 结构对齐，strict 匹配 ✓

[3] Generating submissions...
✓ 推理开始
Generated 1000 submission files               ← 1000 张测试图全部推理完成 ✓
Submission generation complete!
```

**全链路验证通过：**
- ✅ Checkpoint 加载（state_dict 完全匹配，v0.4.1 use_dn 对齐生效）
- ✅ Model Forward
- ✅ DataLoader
- ✅ 后处理
- ✅ 1000 个 submission 文件生成

**结论：** 错误 #9 彻底关闭。训练/推理结构一致性原则已验证——`model.eval()` 模式下 `forward()` 内的 DN 分支不会执行，`use_dn=True` 仅保留参数用于加载对齐。

**注意：** 当前成功推理的是 **debug 模型**（swin_tiny, hidden_dim=128, num_queries=100）。比赛最终提交应使用正式训练 checkpoint：

```bash
python inference.py --checkpoint checkpoints/best.pth --data-root data/test
# 或 checkpoints/latest.pth（Swin-Large, hidden_dim=256, num_queries=900）
```

### 10.9 遗留优化建议（GPT 建议，待实施）

| 建议 | 内容 | 优先级 |
|------|------|:--:|
| 1. 保存 use_dn 到 checkpoint | `train_cfg` 增加 `"use_dn": True`，inference 用 `cfg.get("use_dn", True)` 读取，根治同类问题复发 | 高 |
| 2. AMP API 升级 | `torch.cuda.amp.GradScaler/autocast` → `torch.amp.GradScaler("cuda", ...)` / `autocast("cuda", ...)`，消除 FutureWarning | 低 |

### 10.10 项目里程碑总览（2026-08-06）

| 里程碑 | 状态 |
|--------|:--:|
| 多模态通道不一致修复（96/192） | ✅ |
| Position Encoding 通道修复 | ✅ |
| FPN 通道修复 | ✅ |
| YAML 学习率字符串 → AdamW 报错修复 | ✅ |
| 训练流程稳定进入 Epoch | ✅ |
| Checkpoint 保存与读取 | ✅ |
| DINO use_dn 配置一致 | ✅ |
| **推理全流程 + 1000 提交文件** | ✅ |
| 训练稳定性（Hungarian Matcher 数值） | ⏳ 下一阶段 |
| 模型精度优化 | ⏳ 下一阶段 |

---

## 十一、2026-08-08 联调复盘：错误 #8（验证 0 框 / mAP=0）根因闭环（v0.6.0）

### 11.1 现象时间线

| 时间 | 现象 | 结论 |
|------|------|------|
| rush 训练 20+ 轮 | 验证"预测 0 个框"，loss 下降但 mAP=0 | 后处理 softmax 与 sigmoid Focal 不匹配 + 更深层训练问题 |
| 诊断脚本（首版） | 28800 个 query 的 sigmoid 最大置信度完全一致（0.0515） | 所有 query 输出相同 logits → 模型塌缩 |
| 探针（修复前） | `[DECODER] query_std=0.000000`、[DEP] 随 query 变化 0 | 塌缩点在 Transformer Decoder，且为训练学出 |
| 随机权重探针 | `[DECODER] query_std=0.2375` | 架构本身健康 → 训练动力学问题 |
| input_proj 修复后重训 | epoch 20 探针 `query_std=0.50`、`[BOX] query_std=0.184` | decoder 与框不再塌缩 |
| focal 修复后重训 | epoch 10 验证 2143 框；epoch 20 又 0 框 | 分类进入"全背景"吸引子（类别不平衡） |

### 11.2 根因链（五处问题）

| # | 根因 | 文件 | 证据 |
|---|------|------|------|
| 1 | 位置编码用 `zeros` 累加，所有位置编码相同，decoder 无空间信息 | `models/detector/position_encoding.py` | 代码审查确认；修复后 PE spatial_std=0.115 |
| 2 | `pretrained` 参数被忽略，Swin-Tiny 全程随机初始化 | `models/backbone/rgb_backbone.py` | `timm.create_model(..., pretrained=False)` 硬编码 |
| 3 | `input_proj`（Conv+GroupNorm）定义了但从未接线；FPN 输出 std≈8~11 淹没位置编码（≈0.1），注意力 logits 饱和 → query 训练中塌缩 | `models/detector/dino_detector.py` | `[MEMORY] src spatial_std=8~11 vs pos 0.115`；修复后 `[DECODER] query_std` 0.000006→0.50 |
| 4 | FocalLoss `loss.mean()` 平均到 900×13 元素，分类梯度被稀释约 11700 倍，模型只学框不学分类 | `models/losses/focal_loss.py` | 损失构成：分类 ≈0.06 / 总 1.49；bg logit +1.55、目标类 -3 |
| 5 | 后处理 softmax+argmax 与 sigmoid Focal 不匹配，且忽略背景类竞争 | `engine/evaluator.py`、`inference.py` | 全 query argmax=背景 |

### 11.3 修复清单（git 0998761..a555acb，10 commits）

| commit | 内容 |
|--------|------|
| `dcc2f90` | evaluator/inference 后处理 sigmoid 化；新增 diagnose_predictions.py |
| `a6d10f0` | 位置编码 zeros→ones；pretrained 透传；新增 probe_forward.py |
| `8e55e99` / `2f3656f` | 探针输入尺寸 384×640；[DEP] 依赖测试 |
| `90302fe` | RGBBackbone 支持本地权重路径（无外网服务器）；rush.yaml 注释 |
| `4673c39` | per-class AP 空 stats 崩溃保护；探针 [BOX] 检测 |
| `b13c7a4` | decoder 特征过 input_proj(GroupNorm) 归一化 |
| `817ba2b` | 后处理改"含背景全类 argmax + 阈值"；探针 [BOX] 图像依赖 |
| `dfbcf2d` | FocalLoss 按 query 汇总归一化（分类梯度恢复） |
| `a555acb` | 新增 rush_v2.yaml（queries 300 + focal_alpha 0.5，类别不平衡实验） |

### 11.4 诊断工具（接力常用）

- `python tools/probe_forward.py [--checkpoint xxx]` — 逐环节方差定位塌缩；[DEP] 看 decoder 是否依赖 query/memory；[BOX] 看框是否依赖图像内容
- `python tools/diagnose_predictions.py --checkpoint xxx` — 逐类 logits 均值（背景类 vs 目标类）、置信度分布（raw vs EMA）
- `python evaluate.py --checkpoint checkpoints/rush/latest.pth --data-root data/train [--use-ema]` — 离线评估（新后处理）

### 11.5 错误编号状态更新

| 错误 | 状态 |
|------|:--:|
| #8 验证 0 框 / mAP=0 | 🔴 部分解决：塌缩与后处理已闭环；分类不平衡导致"全背景"反复，rush_v2 验证中 |
| 新增：decoder query 塌缩 | ✅ 已解决（input_proj 归一化） |
| 新增：分类梯度稀释 | ✅ 已解决（FocalLoss 归一化） |
| 新增：类别不平衡 | 🔴 进行中（rush_v2 实验） |

**当前 checkpoint 语义：** `checkpoints/rush/latest.pth` = 最近一轮 20+ 轮（全背景状态，无保留价值）；`best.pth` 仅在 mAP>0 时保存。

### 11.6 接力指南（下一步）

1. **立即跑**：`git pull` 后 `python train.py --config configs/rush_v2.yaml --fold 1`，盯第 10/20 轮验证框数与 mAP。
2. **跑探针**：`python tools/probe_forward.py --checkpoint checkpoints/rush_v2/latest.pth`，看 `[BOX] img1_vs_img2_diff`：
   - 非 0 → 框已依赖图像，聚焦分类（alpha/cost_class/训练时长）；
   - ≈0 → 框仍是模板，改造 decoder 为标准 DETR 接法（tgt=zeros + query_pos 逐层加），并考虑多尺度特征（当前只用 P5）。
3. **分辨率**：实际训练分辨率 384×640（backbone `img_size` 硬编码），配置里 1024×640 未生效；提分辨率需同步改 backbone 与数据集缩放。
4. **注意**：评估/推理用 `latest.pth`（best 只在 mAP>0 时出现）；训练中读 checkpoint 先 `cp` 副本，避免读到写一半的文件。

---

## 十二、2026-08-09 接力核验：按队长交接报告对齐代码（v0.6.1）

### 12.1 本轮目的

本轮先不合入新的结构显存优化实验，而是以队长交接报告为准，确认 v0.6.0 的五处根因修复仍完整存在，避免未验证的架构调整覆盖已经闭环的训练链路修复。

处理原则：

1. **先保持 v0.6.0 baseline 可解释**：先跑 `rush_v2` 验证类别不平衡实验，再做结构优化 A/B；
2. **不要混淆变量**：队长修的是 0 框 / mAP=0 根因链，本轮暂不混入 P2/P3-P5 融合结构改造；
3. **报告直接写入 Markdown**：不再新建 Word 文档，交接说明追加到 `CHANGELOG.md` 和本报告。

### 12.2 五处根因修复核验结果

| # | 根因 | 核验文件 | 当前结果 |
|---|------|----------|----------|
| 1 | 位置编码失效：`cumsum(zeros)` 使所有空间位置编码相同 | `models/detector/position_encoding.py` | 已确认使用 `torch.ones(B, H, W, ...)` 后再 `cumsum`，空间位置编码有效 |
| 2 | `pretrained` 被忽略，rush 配置预训练未生效 | `models/backbone/rgb_backbone.py` | 已确认 `pretrained` 透传 `timm.create_model`；当 `pretrained` 为字符串时支持本地 `.safetensors/.pth` 权重 |
| 3 | decoder 输入特征未归一化，FPN 大数值淹没位置编码 | `models/detector/dino_detector.py` | 已确认 `features[-1]` 先进入 `input_proj[0]`（Conv + GroupNorm），再生成 position encoding 并 flatten 给 decoder |
| 4 | FocalLoss 分类梯度被 `num_queries × num_classes` 稀释 | `models/losses/focal_loss.py` | 已确认返回 `loss.sum(-1).mean()`，先汇总类别维度，再按 query 平均 |
| 5 | 后处理与 sigmoid Focal 训练目标不匹配 | `engine/evaluator.py` / `inference.py` | 已确认使用 `sigmoid + 含背景全类 max/argmax + 过滤背景类 + 阈值` |

### 12.3 工具与配置核验

| 文件 | 用途 | 当前建议 |
|------|------|----------|
| `tools/probe_forward.py` | 定位 query / decoder / box 是否塌缩，重点看 `[DEP]` 和 `[BOX]` | 训练后优先运行 |
| `tools/diagnose_predictions.py` | 查看 logits、背景占比、sigmoid 置信度分布 | 与 probe 配合判断全背景吸引子 |
| `configs/rush_v2.yaml` | 队长留下的类别不平衡实验配置：queries 300 + focal_alpha 0.5 | 下一轮训练 baseline |

建议命令：

```bash
python train.py --config configs/rush_v2.yaml --fold 1
python tools/probe_forward.py --checkpoint checkpoints/rush_v2/latest.pth
python tools/diagnose_predictions.py --checkpoint checkpoints/rush_v2/latest.pth
```

### 12.4 后续最需要注意的点

1. **先跑 `rush_v2`，再跑结构优化版**：如果直接上新结构，mAP 变化无法判断来自类别不平衡、decoder 接法，还是显存优化结构。
2. **`[BOX] img1_vs_img2_diff` 是关键分叉**：
   - 非 0：框已经依赖图像，后续聚焦分类不平衡、loss 权重、训练时长；
   - 约等于 0：框仍是模板，应优先改 decoder 为标准 DETR query_pos 接法。
3. **实际训练分辨率仍需单独处理**：当前训练可能仍是 384×640；配置中的 1024×640 不一定真正生效。
4. **mAP 为 0 时用 `latest.pth`**：`best.pth` 只在 mAP > 0 时保存。
5. **显存优化结构暂不混入主线**：P2 RGB 直通、P3-P5 CMFA、Swin-Small、hidden_dim 192 等方案可作为下一阶段独立实验分支。

### 12.5 阶段结论

当前代码已按队长 v0.6.0 交接报告对齐，五处根因修复均已确认存在。下一步应优先运行 `rush_v2`，验证类别不平衡是否仍导致 epoch 20 回到全背景；只有 baseline 行为清楚后，再引入显存友好的 P3-P5 融合结构做 A/B 实验。

---

## 二十三、2026-08-26 rush_v2 完整 60 轮训练 + 首次非零 mAP + 阈值/NMS 扫描（v0.7.0）

### 23.1 背景

8/23 的 rush_v2 训练在 epoch 17 被中断（见 `.ipynb_checkpoints/rush_v2_train-checkpoint.log`），8/24 notebook 重试因 CUDA 不可见失败（`torch.cuda.is_available()=False`，`rush_v2_train.log` 止于 CUDA 禁用提示）。8/26 容器重启后 GPU 恢复（RTX 5090；base python torch 2.12.1+cu130），且当时无任何训练进程在跑。

### 23.2 本轮操作（交接记录）

1. 从 `checkpoints/rush_v2/latest.pth`（epoch 15）续训至 60/60 轮全部完成，日志 `rush_v2_resume_train.log`（约 30 秒/epoch，约 35 分钟跑完剩余 46 轮）。
2. 对最终模型跑诊断：`tools/diagnose_predictions.py`、`tools/diagnose_iou.py`。
3. 跑阈值/NMS 扫描：`tools/sweep_eval.py`（40 组，全量训练集），日志 `sweep_rush_v2.log`。
4. 用扫描最佳配置生成测试提交 `submission_rush_v2_ep60.zip`。

### 23.3 训练与验证结果

| 指标 | epoch 15（续训起点） | epoch 60（结束） |
|------|------|------|
| loss | 2.45 | 1.84 |
| cls | 0.093 | 0.074 |
| ce | 1.03 | 0.59 |
| bbox | 0.042 | 0.025 |
| giou | 0.81 | 0.67 |

验证（EMA，每 10 轮一次）：

| epoch | mAP50-95 | mAP50 |
|-------|----------|-------|
| 20 | 0.0001 | 0.0004（历史首次非零） |
| 30 | 0.0000 | 0.0000 |
| 40 | 0.0000 | 0.0003 |
| 60 | 0.0001 | 0.0007（New Best） |

checkpoint：`checkpoints/rush_v2/{best,latest,final}.pth`（8/26 07:30，605M，cfg: swin_tiny / hidden 256 / 300 queries / use_dn=False）。

### 23.4 诊断结论

- 类别分布：pred 仅 class 10/8/0/6；GT 中 class 4（33 个）与 class 3（2 个）完全没出 → 少数类召回缺失。
- 定位：GT recall@IoU0.5（同类别）= 22.5%；每图最佳框 IoU ≈ 0.53，但最高分框平均 IoU 仅 0.14 → 置信度排序与定位质量脱节。
- 框形态：预测框偏高偏大（h 均值 0.157 vs GT 0.114）、中心偏右（cx 0.55 vs 0.48）。
- 置信度：去背景 sigmoid 均值约 0.21（EMA 约 0.24）；conf 大于 0.5 的 query 仅 0~3.6%。

### 23.5 阈值/NMS 扫描结果（40 组）

| conf | nms | preds | mAP50-95 | mAP50 |
|------|-----|-------|----------|-------|
| 0.001 | 0.50 | 198802 | **0.0022** | **0.0096** |
| 0.001 | 0.60 | 199820 | 0.0021 | 0.0092 |
| 0.080 | 0.60 | 196324 | 0.0020 | 0.0077 |
| 0.005 | 0.50 | 198795 | 0.0020 | 0.0085 |

结论：低阈值 + nms 0.5~0.6 最优；提高阈值不改善 mAP（置信度排序差）。注意 sweep 在含训练图的 2000 张上评估，数值偏乐观，仅用于配置间相对比较。

### 23.6 提交

- `submission_rush_v2_ep60/`：1000 个 txt（格式 `class cx cy w h conf`），平均约 99.7 框/图。
- `submission_rush_v2_ep60.zip`（1.9MB），可直接上传。
- 使用 raw model（非 EMA）与 sweep 一致；如需对比可加 `--use-ema` 再生成一版。

### 23.7 接力建议

1. 管线全通、训练收敛正常，但模型精度仍为 mAP 约 0.1~0.2%（验证/训练集）量级；主要瓶颈是**定位精度、少数类召回、置信度标定**。
2. 定位：检查 box head / 提高 GIoU 权重或延长训练；换更大预训练 backbone（swin_large）做 A/B。
3. 少数类：class 4/3 样本极少，考虑类别重采样、复制增强或 focal 调参。
4. 置信度排序：考虑 score 校准或排序损失。
5. 用 EMA checkpoint 与 raw 各出一版提交对比；GPU 当前空闲可直接续跑；容器重启会杀掉 nohup 进程，但 checkpoint 每 5 轮保存。

---
## 二十二、2026-08-22 正样本 CE 分类辅助损失（v0.6.11）

### 22.1 新证据

v0.6.10 后类别分布变为：

```text
pred: [(6, 6400)]
gt:   [(10, 102), (6, 68), (8, 67), ...]
```

这说明手写类别权重虽然打破了 class 0/2，但又把模型推向 class 6。继续调固定类别权重风险较高。

### 22.2 已实施修复

新增正样本 CE 分类辅助损失：

```python
matched_logits.append(pred_logits[i, src_idx, :num_classes])
matched_labels.append(target_labels[i][tgt_idx])
loss_ce = F.cross_entropy(matched_logits, matched_labels)
```

总损失变为：

```text
loss = cost_class * focal + cost_ce * CE + cost_bbox * L1 + cost_giou * GIoU
```

配置调整：

```yaml
cost_class: 1.0
cost_ce: 0.5
cost_bbox: 5.0
cost_giou: 2.0
focal_alpha: 0.5
```

### 22.3 后续验证

训练日志会新增：

```text
ce=...
```

重新训练后重点看 `pred` 类别分布是否从单一 class 6 扩散到多个真实类别，尤其是 class 10/8/6。

---

## 二十一、2026-08-22 类别权重正样本化修复（v0.6.10）

### 21.1 新证据

v0.6.9 后评估出现：

```text
mAP@50: 0.0001
```

这说明完整检测链路已经不是严格 0，但类别分布仍旧：

```text
pred: [(0, 6336), (2, 64)]
gt:   [(10, 102), (6, 68), (8, 67), ...]
```

### 21.2 根因修正

v0.6.9 的 `class_weights` 直接乘到 focal loss 的所有位置。对于 sigmoid focal，未匹配 query 的每个前景类都是负样本。因此提高 class 10/6/8 权重时，也会放大这些类在海量 unmatched query 上的负样本惩罚，可能继续压低少数类 logit。

### 21.3 已实施修复

`models/losses/focal_loss.py` 中类别权重改为只作用于正样本：

```python
pos_weights = 1.0 + (weights.view(1, -1) - 1.0) * targets
loss = loss * pos_weights
```

负样本项保持权重 1，避免少数类在未匹配 query 上被额外压制。

### 21.4 下一步验证

```bash
python train.py --config configs/debug.yaml --fold 1
python tools/diagnose_iou.py --checkpoint checkpoints/debug/latest.pth --conf-threshold 0.01
python tools/diagnose_predictions.py --checkpoint checkpoints/debug/latest.pth
python evaluate.py --checkpoint checkpoints/debug/latest.pth --data-root data/train --conf-threshold 0.01
```

若 `pred` 类别分布开始出现 class 10/6/8，说明分类塌缩缓解。

---

## 二十、2026-08-22 类别塌缩修复：class-weighted focal（v0.6.9）

### 20.1 新证据

加强定位损失后，IoU 诊断出现改善：

```text
每图最佳 IoU(不看类别): 0.3994
每图最佳 IoU(同类别):   0.2335
GT recall@IoU0.5: any=0.0730 same_cls=0.0730
conf > 0.1: 923 / 9600
```

但类别分布仍然塌缩：

```text
pred: [(0, 6226), (2, 174)]
gt:   [(10, 102), (6, 68), (8, 67), (0, 43), ...]
```

说明当前瓶颈已经从“完全没有可用框”转为“类别预测过度集中到 class 0/2”。

### 20.2 已实施修复

| 文件 | 修复内容 |
|------|----------|
| `models/losses/focal_loss.py` | 新增 `class_weights` buffer，对前景类别 focal loss 逐类加权 |
| `models/losses/dino_loss.py` | `DINOLoss` 新增 `class_weights` 参数并透传 |
| `train.py` | 从 `loss.class_weights` 读取配置 |
| `configs/debug.yaml` / `configs/rush_v2.yaml` | `cost_class` 提高到 2.0，`focal_alpha` 提高到 0.75，并加入类别权重 |

当前权重：

```yaml
class_weights: [0.5, 1.0, 0.7, 1.2, 1.2, 1.0, 2.0, 1.0, 2.0, 1.2, 2.5, 1.0]
```

### 20.3 后续判断

重新训练 debug 后执行：

```bash
python tools/diagnose_iou.py --checkpoint checkpoints/debug/latest.pth --conf-threshold 0.01
python tools/diagnose_predictions.py --checkpoint checkpoints/debug/latest.pth
```

若 `pred` 类别分布开始覆盖 class 10/6/8，说明类别塌缩缓解；若仍集中 class 0/2，则应进一步考虑基于完整训练集频次自动计算 class-balanced 权重，或检查 label 映射是否和评估类别 id 一致。

---

## 十九、2026-08-22 定位损失重新加权（v0.6.8）

### 19.1 新证据

IoU 诊断显示：

```text
每图最佳 IoU(不看类别): 0.1735
每图最佳 IoU(同类别):   0.0483
GT recall@IoU0.1: any=0.2857 same_cls=0.0698
GT recall@IoU0.3: any=0.0159 same_cls=0.0000
GT recall@IoU0.5: any=0.0000 same_cls=0.0000
pred: [(0, 6336), (2, 64)]
gt:   [(10, 102), (6, 68), (8, 67), (0, 43), ...]
```

该结果说明 mAP@50 为 0 的直接原因是：没有同类别预测框达到 IoU 0.5。进一步拆分看，框定位本身也很弱，class-agnostic 的 IoU@0.5 recall 已经是 0。

### 19.2 已实施调整

| 文件 | 调整 |
|------|------|
| `configs/debug.yaml` | `cost_bbox: 2.0 → 5.0`，`cost_giou: 1.0 → 2.0` |
| `configs/rush_v2.yaml` | 同步恢复更强定位权重 |
| `tools/diagnose_iou.py` | 框分布增加逐坐标 `cx/cy/w/h` 的 mean/min/max |

### 19.3 为什么仍能控制 loss

当前分项大致为：

```text
cls≈0.10 bbox≈0.048 giou≈0.80
```

调整后总 loss 约为：

```text
1*0.10 + 5*0.048 + 2*0.80 ≈ 1.94
```

因此定位权重增强后，总 loss 仍大概率处于 1~2 左右，但优化压力会更多落到 bbox/GIoU 上。

### 19.4 后续判断

重新训练 debug 后：

```bash
python tools/diagnose_iou.py --checkpoint checkpoints/debug/latest.pth --conf-threshold 0.01
```

若 `GT recall@IoU0.1/0.3` 上升，说明定位方向有效；若 `any IoU` 上升但 `same_cls` 仍很低，再进入类别塌缩修复。若 `any IoU` 仍接近 0，则应检查 dataset 的 box 坐标格式与 resize 后归一化逻辑。

---

## 十八、2026-08-21 IoU 诊断工具补充（v0.6.7）

### 18.1 新增背景

v0.6.6 后 debug 训练 loss 已进入约 1 的区间，分项示例：

```text
avg=0.9953 cls=0.0996 bbox=0.0482 giou=0.7993
```

同时诊断显示前景置信度开始抬升：

```text
去背景后 sigmoid 最大置信度: mean=0.0976 max=0.1000
conf > 0.1: 7 / 9600
```

但 mAP@50 仍为 0，说明下一步重点应从“loss 数值”转向“预测框是否和 GT 有足够 IoU、类别是否一致”。

### 18.2 新增工具

新增 `tools/diagnose_iou.py`，用于输出：

1. Top1 预测框与 GT 的最大 IoU；
2. 每图 top-k 中的最佳 IoU；
3. 不看类别与同类别两套 IoU；
4. GT recall@IoU0.1/0.3/0.5；
5. 预测框与 GT 框的 `(cx, cy, w, h)` 分布；
6. 预测类别和真实类别分布。

### 18.3 使用命令

```bash
python tools/diagnose_iou.py --checkpoint checkpoints/debug/latest.pth --conf-threshold 0.01
```

### 18.4 判断标准

| 观察结果 | 结论 | 下一步 |
|----------|------|--------|
| `每图最佳 IoU(不看类别)` 接近 0 | 框定位或坐标尺度有问题 | 检查 dataset box 归一化、resize、box head 输出范围 |
| 不看类别 IoU 较高，但同类别 IoU 接近 0 | 框能对上但类别错 | 调整分类 loss、类别不平衡、label 映射 |
| `GT recall@IoU0.1` 有值但 `IoU0.5` 为 0 | 框大致在目标附近但不准 | 加强 bbox/GIoU 权重或训练更久 |
| 预测框 `w/h` 分布明显小于或大于 GT | 框尺寸偏置 | 检查 box head 初始化、GT 格式、loss 权重 |

---

## 十七、2026-08-21 loss 量纲与评估候选数调整（v0.6.6）

### 17.1 问题背景

当前主要症状变为：

1. mAP 仍为 0；
2. 总 loss 偏高，用户希望控制在 1~2；
3. 评估阶段候选框数量过多，debug 阶段曾出现每图 900 个 query 全释放。

需要注意：总 loss 的绝对数值不是评价检测模型好坏的唯一标准。`class/bbox/giou` 三项权重和归一化方式都会改变总 loss 数值。若只把权重整体缩小，loss 可以变低，但 mAP 不会自然变好。

### 17.2 已实施修复

| 文件 | 修复内容 |
|------|----------|
| `models/losses/focal_loss.py` | Focal loss 从 `loss.sum()` 改为先 `loss.mean(-1)`，再按 GT 数量归一化，避免类别数把分类项放大 |
| `engine/trainer.py` | 日志新增 `cls/bbox/giou` 分项平均值 |
| `engine/evaluator.py` | 每张图最多保留 top100 预测框，与 COCOeval `maxDets=100` 对齐 |
| `configs/debug.yaml` | debug 从 `swin_large + 900 queries` 改为 `swin_tiny + 300 queries`，并设置 `num_workers=2` |
| `configs/debug.yaml` / `configs/rush_v2.yaml` | loss 权重调整为 `cost_class=1.0, cost_bbox=2.0, cost_giou=1.0` |

### 17.3 为什么这样改

1. **分类 loss 量纲修正**  
   当前使用 sigmoid focal，只监督前景 12 类。如果直接对类别维度求和，分类项会随类别数放大。改为类别维度平均后，总 loss 更接近可解释范围。

2. **分项日志避免误判**  
   只看 `loss=5` 无法判断是分类高、框 L1 高，还是 GIoU 高。新增 `cls/bbox/giou` 后，可以按具体项继续定位。

3. **减少低质量候选干扰**  
   COCOeval 默认每图最多看 100 个检测，评估前保留 top100 可以减少大量低分候选造成的计算开销，也让输出更接近最终提交逻辑。

4. **debug 配置回到轻量实验**  
   `swin_large + 900 queries` 不适合作为快速排错配置，负样本比例和显存压力都过高。debug 应优先验证链路是否恢复，而不是追求大模型。

### 17.4 下一步验证

```bash
python train.py --config configs/debug.yaml --fold 1
```

训练时观察类似日志：

```text
loss=... avg=... cls=... bbox=... giou=...
```

判断：

1. 若 `avg` 进入 1~2 且 `cls` 下降，说明 loss 量纲和分类目标基本正常；
2. 若 `bbox/giou` 仍高，优先检查坐标格式、resize 后 box 是否同步；
3. 若 `cls` 很低但 mAP 仍 0，优先查 IoU；
4. 若 `cls` 仍高且前景置信度不升，继续调 `focal_alpha`、queries 和正负样本策略。

---

## 十六、2026-08-20 sigmoid focal 背景监督移除（v0.6.5）

### 16.1 新增判断

v0.6.3 已让评估/推理阶段忽略背景类，只对前景类别做 sigmoid 最大分数筛选；v0.6.4 已让 decoder query 开始分化。但训练损失中仍保留显式背景类监督：

```python
target_classes = torch.full((B, Q), self.num_classes)
target_onehot.scatter_(1, target_classes.reshape(-1, 1), 1)
```

由于每张图真实目标数量远小于 query 数量，绝大多数 query 都被监督为背景类正样本。这与当前 matcher/evaluator/inference 的“忽略背景类，仅使用前景 sigmoid”目标不一致，会继续强化背景 logit，造成前景置信度难以抬升。

### 16.2 已实施修复

| 文件 | 修复内容 |
|------|----------|
| `models/losses/dino_loss.py` | 分类 target 改为 `(B, Q, num_classes)`，只包含前景类 |
| `models/losses/dino_loss.py` | 未匹配 query 不再设置 `background=1`，而是所有前景类均为 0 |
| `models/losses/dino_loss.py` | 计算 focal loss 时只使用 `pred_logits[:, :, :num_classes]`，保留最后一维背景 logit 以兼容旧结构和 checkpoint |

修复后的核心逻辑：

```python
target_onehot = torch.zeros(B, Q, self.num_classes, device=pred_logits.device)
target_onehot[i, src_idx, labels] = 1
loss_class = focal_loss(
    pred_logits[:, :, :self.num_classes].reshape(-1, self.num_classes),
    target_onehot.reshape(-1, self.num_classes),
    normalizer=num_boxes,
)
```

### 16.3 后续验证

该修改会改变分类训练目标，因此必须重新训练后再判断 mAP：

```bash
python train.py --config configs/debug.yaml --fold 1
python tools/diagnose_predictions.py --checkpoint checkpoints/debug/latest.pth
python tools/probe_forward.py --checkpoint checkpoints/debug/latest.pth
python evaluate.py --checkpoint checkpoints/debug/latest.pth --data-root data/train --conf-threshold 0.01
```

判断重点：

1. 前景 sigmoid 置信度是否高于旧值 `max≈0.0537`；
2. `conf > 0.1` 的 query 是否开始出现；
3. `softmax argmax == 背景` 可作为参考，但不再是推理筛选依据；
4. 若前景置信度抬升但 mAP 仍为 0，下一步检查候选框 IoU 和坐标尺度。

---

## 十五、2026-08-20 decoder query 塌缩修复（v0.6.4）

### 15.1 新增现场证据

在 v0.6.3 修复后，评估预测框从 0 变为：

```text
预测框: 1800000
真实框: 14417
```

说明“无预测框”问题已经解除。但 mAP 仍为 0，因此继续执行：

```bash
python tools/probe_forward.py --checkpoint checkpoints/debug/latest.pth
```

得到关键输出：

```text
[DECODER]   query_std=0.000121
[DEP]       hs 随 memory 变化: 0.209253 | hs 随 query 变化: 0.000135
[LOGITS]    query_std=0.000062 img1_vs_img2_diff=0.499368
[BOX]       query_std=0.000021 img1_vs_img2_diff=0.154687
```

该结果说明 decoder 输出几乎不随 object query 改变。虽然不同图像会产生不同输出，但同一张图里的 900 个 query 基本生成同一套 logits 和 box，因此 COCOeval 中大量候选实际是重复模板，难以匹配真实目标。

### 15.2 根因判断

`dino_detector.py` 中创建了：

```python
tgt = torch.zeros_like(query_embed)
```

但 `models/detector/transformer.py` 旧实现没有真正使用这个 `tgt`，而是直接执行：

```python
hs = self.decoder(query_embed, memory, tgt_mask=tgt_mask)
```

这会把 object query 当作 decoder 内容输入，而不是按 DETR/DINO 的方式作为 query position 注入注意力。训练后在 cross-attention 和 LayerNorm 作用下，多个 query 很容易被同一 memory 表征拉成近似相同的输出。

### 15.3 已实施修复

| 文件 | 修复内容 |
|------|----------|
| `models/detector/transformer.py` | 新增 `DINOTransformerDecoderLayer`，在 self-attention 中使用 `q=k=tgt+query_pos, v=tgt` |
| `models/detector/transformer.py` | cross-attention 中使用 `q=tgt+query_pos, k=memory+pos, v=memory` |
| `models/detector/transformer.py` | 保留 `decoder.layers.*`、`self_attn`、`multihead_attn`、`linear/norm/dropout` 等旧 checkpoint 键名 |

修复后的关键逻辑：

```python
q = k = tgt + query_pos
tgt2 = self.self_attn(q, k, value=tgt, attn_mask=tgt_mask)[0]
tgt2 = self.multihead_attn(
    query=tgt + query_pos,
    key=memory + pos,
    value=memory,
)[0]
```

### 15.4 后续验证

先做结构诊断：

```bash
python -m py_compile models/detector/transformer.py
python tools/probe_forward.py --checkpoint checkpoints/debug/latest.pth
```

若 `[DECODER] query_std`、`[DEP] hs 随 query 变化`、`[BOX] query_std` 明显抬升，说明 query 塌缩被解除。由于 decoder 前向语义已经改变，旧 checkpoint 只能用于诊断结构是否分化；正式 mAP 需要重新训练：

```bash
python train.py --config configs/rush_v2.yaml --fold 1
```

---

## 十四、2026-08-20 评估 0 框的后处理补丁（v0.6.3）

### 14.1 新增现场证据

服务器使用 `checkpoints/debug/latest.pth` 执行：

```bash
python evaluate.py --checkpoint checkpoints/debug/latest.pth --data-root data/train --conf-threshold 0.001
python tools/diagnose_predictions.py --checkpoint checkpoints/debug/latest.pth
```

得到关键输出：

```text
预测框: 0
真实框: 14417
softmax argmax == 背景 的比例: 1.0000
去背景后 sigmoid 最大置信度: mean=0.0506 max=0.0537
```

这说明模型前景类并不是完全无响应。即使 debug 只训练 5 个 epoch，前景 sigmoid 最高值已经达到约 0.0537，理论上在 `conf_threshold=0.001` 下应该能释放候选框。

### 14.2 根因判断

v0.6.2 的后处理仍采用：

```python
probs = pred_logits.sigmoid()
confs, labels = probs.max(-1)
keep = (labels < num_classes) & (confs > conf_threshold)
```

该逻辑把背景类也纳入最大分数竞争。当前模型背景 logit 明显高于前景 logit，导致每个 query 的 `labels` 都落到背景类，随后被 `labels < num_classes` 过滤掉。结果是前景类虽然有低置信候选，但评估和推理阶段全部丢弃，最终表现为 `预测框: 0`。

### 14.3 已实施修复

| 文件 | 修复内容 |
|------|----------|
| `engine/evaluator.py` | `sigmoid()` 后只保留前景 12 类参与 `max`，背景类不参与评估筛选 |
| `inference.py` | 提交文件生成逻辑同步改为仅前景类取最大 sigmoid 分数 |

修复后的核心逻辑：

```python
probs = pred_logits.sigmoid()[:, :-1]
confs, labels = probs.max(-1)
keep = confs > conf_threshold
```

### 14.4 后续判断

修复后应先复用同一个 debug checkpoint 重新评估：

```bash
python evaluate.py --checkpoint checkpoints/debug/latest.pth --data-root data/train --conf-threshold 0.001
```

若 `预测框` 由 0 变为非 0，说明“0 框”来自后处理过滤，而不是模型完全无输出。之后若 mAP 仍为 0，应继续检查：

1. 候选框与 GT 的 IoU 是否过低；
2. 坐标格式和图像缩放是否一致；
3. 预测类别是否集中到少数类；
4. debug 训练 epoch 太少导致定位仍未收敛。

---

## 十三、2026-08-14 mAP 持续为 0 的剩余链路修复（v0.6.2）

### 13.1 问题定位

在 v0.6.0 修复后，模型已经从“完全 0 框 / query 塌缩”推进到“部分 epoch 能出框，但 mAP 仍为 0 或后期重新全背景”。这说明底层 forward 和 decoder 分化已经有所恢复，但训练匹配、分类信号和评估过滤仍可能把结果压回 0。

本轮聚焦三个更靠后的环节：

1. Hungarian matcher 的分类代价是否和 sigmoid Focal Loss 一致；
2. 分类损失是否仍被 query 数量稀释；
3. mAP 评估是否在 COCOeval 前用过高阈值提前丢掉候选框。

### 13.2 发现的问题

| 问题 | 位置 | 影响 |
|------|------|------|
| matcher 仍使用 `softmax` 分类代价 | `models/detector/matcher.py` | 与 sigmoid Focal Loss 不一致，背景类会和目标类强制竞争，类别不平衡时容易匹配到低质量 query |
| 分类 loss 仍按 `B × Q` 平均 | `models/losses/focal_loss.py` / `models/losses/dino_loss.py` | 虽然已避免除以 `Q × C`，但单图 GT 很少时，正样本信号仍会被 query 数量压弱 |
| 评估默认阈值 `0.3` 偏高 | `engine/evaluator.py` / `evaluate.py` | COCO AP 依赖候选排序，评估前高阈值会提前过滤低置信候选，导致 mAP 被人为压成 0 |
| 预测框缺少评估前数值保护 | `engine/evaluator.py` | 越界框、极小宽高可能干扰 COCO 格式评估 |

### 13.3 已实施修复

| 文件 | 修复内容 |
|------|----------|
| `models/detector/matcher.py` | 分类代价由 `pred_logits.softmax(-1)` 改为 `pred_logits.sigmoid()[:, :-1]` 的目标类概率；与 sigmoid Focal 训练目标对齐 |
| `models/detector/matcher.py` | GIoU 的 `union` / `enclose` 分母增加 `eps`，总代价矩阵进入 Hungarian 前使用 `torch.nan_to_num` |
| `models/losses/focal_loss.py` | `forward()` 新增 `normalizer` 参数，支持外部传入 GT 数量做归一化 |
| `models/losses/dino_loss.py` | 统计 batch 内 `num_boxes`，分类 loss 按 GT 数量归一化，增强正样本分类梯度 |
| `engine/evaluator.py` | `evaluate_model()` / `validate()` 默认 `conf_threshold` 从 `0.3` 降为 `0.001` |
| `engine/evaluator.py` | 预测框转换 COCO xywh 前先 clamp 到 `[0, 1]`，转换后限制坐标和最小宽高 |
| `evaluate.py` | 新增 `--conf-threshold` 参数，支持命令行显式控制评估过滤阈值 |

### 13.4 为什么这些修改有助于 mAP 非 0

1. **匹配质量更接近训练目标**  
   当前分类头使用 sigmoid Focal Loss。matcher 如果继续使用 softmax，会把背景类和目标类做互斥竞争；但 sigmoid Focal 的语义是每个类别独立判断。改成 sigmoid 目标类概率后，匹配阶段和训练阶段的分类信号一致。

2. **正样本分类梯度更强**  
   原来的 `loss.sum(-1).mean()` 仍然对所有 query 平均。对于 300 queries、约 7 个 GT 的图像，正样本分类信号仍然偏弱。本轮改为按 batch 内 GT 数量归一化，更接近 DETR 系列常见做法。

3. **评估不再过早丢候选**  
   mAP 计算本质上依赖预测框按 score 排序后的 PR 曲线。模型早期即使置信度只有 0.02~0.2，也可能已经有粗略定位能力。若评估前固定阈值 0.3，这些候选会被全部丢弃，mAP 就会显示为 0。默认改为 0.001 后，更适合判断模型是否开始学到有效检测。

### 13.5 下一步验证命令

建议先重新训练 `rush_v2`：

```bash
python train.py --config configs/rush_v2.yaml --fold 1
```

训练中或训练后评估：

```bash
python evaluate.py --checkpoint checkpoints/rush_v2/latest.pth --data-root data/train --conf-threshold 0.001
```

诊断 query 和框是否依赖图像：

```bash
python tools/probe_forward.py --checkpoint checkpoints/rush_v2/latest.pth
```

诊断类别分布：

```bash
python tools/diagnose_predictions.py --checkpoint checkpoints/rush_v2/latest.pth
```

### 13.6 判断分支

| 观察结果 | 结论 | 下一步 |
|----------|------|--------|
| `预测框数量` 非 0，`mAP@50` 先出现非 0 | 训练链路开始恢复 | 继续训练，观察 mAP@50-95 是否跟进 |
| `[BOX] img1_vs_img2_diff` 明显非 0，但 mAP 仍 0 | 框依赖图像，但分类或框精度不足 | 调整 `cost_class`、`focal_alpha`、训练时长和数据增强 |
| `[BOX] img1_vs_img2_diff` 约等于 0 | 框仍是模板 | 优先改 decoder 为标准 DETR query_pos 接法 |
| 目标类 sigmoid 始终低，背景类持续高 | 全背景吸引子仍在 | 继续增强正样本分类权重，考虑降低 queries 或调整 focal/no-object 策略 |
| 预测框很多但 IoU 极低 | 定位质量不足 | 检查 GT 坐标格式、图像缩放、box loss / GIoU 数值 |

### 13.7 阶段结论

本轮修复的是 v0.6.0 之后仍可能导致 mAP 为 0 的后半段链路：matcher 与 loss 不一致、分类归一化仍偏弱、评估阈值过高以及坏框数值保护不足。优先级上，应先用这些修改重跑 `rush_v2`，看是否能让 `mAP@50` 率先脱离 0；若 `[BOX] img1_vs_img2_diff` 仍接近 0，再进入 decoder query_pos 改造。

---

## 二十三、2026-08-28 方向决策：无卡环境切换 YOLO 早期融合方案（v0.8.0）

### 23.1 决策背景

1. **服务器当前为无卡模式（无 GPU）**：`torch.cuda.is_available() == False`，`/usr/bin/nvidia-smi` 是 0 字节占位文件；`rush_v9_anchor900_multiscale`（v0.7.7）在构建模型后即中断，无法继续训练。
2. **原 M3F-DETR 方案长期无法出分**：即使有卡时，`rush_v2` ~ `rush_v9` 验证集 `mAP@50-95` 最高约 0.005，大部分实验在 0.0000~0.0032 之间；根因集中在"类别塌缩 + 置信度排序失真"（score–IoU 相关性仅 0.176），一个月的排错已证明自研检测链路难以在有限时间内收敛。
3. **赛程约束**：算法挑战赛道复赛 2026-09 中旬前开始，初赛提交窗口预计仅剩 1~3 周；赛题规定成绩低于官方基线将被判无效，当前量级（~0.005）几乎必然无效，必须尽快切换到可快速出分的路线。

### 23.2 为什么选择 YOLO 早期融合方案

1. **结构性消除自研链路的历史问题**。此前 NHWC/NCHW、decoder query 塌缩、matcher/loss 不一致、CMFA OOM、背景类吞框等问题，全部源于"从零手写 Swin + CMFA + FPN + DINO"且无参考实现可对照。YOLO11 是工业级、训练/推理/损失同源的实现，这些坑在架构层面不会出现。
2. **自研改动面最小**。采用"早期融合"：RGB(3ch) + 红外灰度(1ch) + 深度归一化(1ch) 拼接为 5 通道输入，只需把首层卷积从 3 通道扩到 5 通道；其余 backbone、neck、head、损失全部用官方实现。
3. **预训练权重可用且迁移路径清晰**。允许使用 COCO/ImageNet 公开预训练权重；`yolo11n.pt` 的 3 通道首层权重复制到新通道（均值初始化），分类头因类别数从 80→12 从零训练，符合常规微调实践。
4. **控制变量验证**。先跑 RGB-only 对照（同一套代码，`mode=rgb`），确认环境、数据、评估链路无误后，再叠加 5 通道融合；任何一环 mAP 为 0 都能立刻定位到"我们自己的胶水代码"，不会再次陷入整链排错。
5. **与赛题评分口径兼容**。YOLO 输出按置信度排序、每图 top-100 + 同类别 NMS，再转 COCO 格式用 pycocotools 计算 mAP@50-95（101 点插值），与官方评测逻辑一致。

### 23.3 具体实施步骤

1. **恢复 GPU 并安装依赖**：AutoDL 实例重新开卡（或换实例），在 `race` 环境安装 `ultralytics`（2026-08-28 已完成，版本 8.4.131）。
2. **新增数据集模块 `yolo/dataset.py`**：读取 `data/train|test/{visible,infrared,depth,labels}`，按 visible 目录为基准做 stem 映射；输出 3ch（RGB-only）或 5ch（RGB+IR灰度+Depth归一化）张量 + YOLO 归一化标签；测试集无标签返回空目标；同步增强（resize + 随机水平翻转，RGB 额外做光度扰动）。
3. **新增模型模块 `yolo/model.py`**：`build_yolo_model()` 用 `yolo11n.yaml` + `ch=5/3` + `nc=12` 构建模型，从 `yolo11n.pt` 迁移形状匹配的权重；首层卷积 5 通道的额外通道用 RGB 权重均值初始化；保存时写入 `cfg`（model/ch/nc/imgsz/mode）供推理恢复。
4. **新增训练配置** `configs/yolo_rgb.yaml` / `configs/yolo_fusion.yaml`：输入尺寸、epochs、batch、lr、EMA、AMP、冻结策略、checkpoint 目录。
5. **新增训练入口 `tools/train_yolo.py`**：沿用 `train.py` 风格（YAML 配置、`[1]~[5]` 分段日志、EMA、AMP、best/latest/final 三份 checkpoint）；训练循环直接调用官方模型前向 `model(batch)` 获得损失，不绕过官方损失。
6. **新增推理入口 `tools/infer_yolo.py`**：测试集推理 → NMS → 每图最多 100 框 → 按官方格式写 `[class_id cx cy w h confidence]` 的 txt → 打包 zip。
7. **验证顺序**：
   - 小数据过拟合冒烟：20 张图训练 loss 必须下降，否则先查胶水代码；
   - `mode=rgb` 对照训练：验证集 mAP@50-95 必须非 0；
   - `mode=fusion` 训练：与 RGB-only 对比，确认多模态增益；
   - 提交：`tools/infer_yolo.py` 生成 1000 个 txt 并打包。

### 23.4 涉及修改/新增的模块或文件

| 文件 | 类型 | 说明 |
|------|------|------|
| `yolo/__init__.py` | 新增 | YOLO 方案包入口 |
| `yolo/dataset.py` | 新增 | 三模态数据加载、5ch/3ch 输出、同步增强、collate |
| `yolo/model.py` | 新增 | 模型构建、权重迁移、首层卷积 3→5 扩展 |
| `yolo/evaluate.py` | 新增 | YOLO 推理解码 + COCO mAP@50-95 评估（复用 engine/evaluator.compute_map） |
| `tools/train_yolo.py` | 新增 | YOLO 训练入口 |
| `tools/infer_yolo.py` | 新增 | YOLO 推理 + 提交文件生成 |
| `configs/yolo_rgb.yaml` | 新增 | RGB-only 对照配置 |
| `configs/yolo_fusion.yaml` | 新增 | 5ch 早期融合配置 |
| `requirements.txt` | 修改 | 增加 `ultralytics` |
| `CHANGELOG.md` | 修改 | 本版本记录（v0.8.0） |
| 本报告 | 修改 | 本节 |

> M3F-DETR 旧链路（`train.py`、`models/`、`engine/`、`configs/rush_*.yaml` 等）**保留冻结**，不再作为主训练路径，但保留作为技术报告"问题与思考"素材与对照参考，不删除。

### 23.5 交接注意事项（接手者必读）

1. **环境**：训练/推理统一使用 `/root/miniconda3/envs/race/bin/python`（已装 ultralytics 8.4.131）。无卡模式只能写代码与 CPU 冒烟，正式训练前必须先恢复 GPU。
2. **数据**：`data/train` 2000 组（含 `labels/`），`data/test` 1000 组（无标签）；目录固定为 `visible/infrared/depth/labels`；标签为 YOLO 归一化格式 `[cls cx cy w h]`（少量越界需 clamp）；Depth 为 16bit 毫米值，有效范围 300~19999，`0` 为无效需掩码。
3. **提交格式**：每张测试图一个同名 txt，行格式 `[class_id cx cy w h confidence]`，每图最多 100 框，空图也必须提交空 txt，全部打包为 zip；非法类别/坐标/缺失置信度会导致该预测无效。
4. **复现顺序**：`tools/train_yolo.py --config configs/yolo_rgb.yaml` 先出 RGB 对照分，再 `--config configs/yolo_fusion.yaml` 出融合分；两份配置的差异只有 `dataset.mode`（rgb/fusion，决定输入通道数 3/5）。
5. **已知坑**：
   - 不要用 `cv2.imread` 直接读 5 通道；5 通道只在内存中拼接；
   - 首层卷积新增通道必须做权重初始化（RGB 均值复制），否则融合分支梯度消失；
   - 推理时模型输入必须是训练同款归一化（RGB 除以 255、IR 单通道除以 255、Depth 归一化后单通道）；
   - 评估用 `engine/evaluator.py::compute_map`（pycocotools，mAP@50-95），预测框先按置信度降序、每图 top-100、同类别 NMS。
6. **代码风格**：与现有项目保持一致（中文 docstring、argparse + YAML 配置、`[n]` 分段日志、checkpoint 内保存 `cfg`）。
7. **Git 纪律**：改动先更新本文档与 CHANGELOG，再提交并推送 `origin/main`；远程/本地仓库需保持一致（当前远程 HEAD `ddb83ad`，本地需先 `git pull`）。

### 23.6 已完成实现与验证（2026-08-28）

1. **代码已落地**：`yolo/` 包、`tools/train_yolo.py`、`tools/infer_yolo.py`、`configs/yolo_rgb.yaml`、`configs/yolo_fusion.yaml`、`requirements.txt`（+ultralytics）均已提交本版本。
2. **依赖已安装**：race 环境 ultralytics 8.4.131；`yolo11n.pt` 已下载到项目根目录。
3. **无卡环境冒烟验证通过**（迷你数据集 8 张、CPU、320×192）：
   - 数据加载：fusion 输出 `(5,384,640)`，RGB/IR/Depth 三通道统计正常；深度图按 8bit JPG/16bit PNG 兼容归一化；
   - 模型：`ch=5, nc=12` 构建成功，447/52 权重键迁移（stem 与分类头跳过），新通道用 RGB 均值初始化；
   - 训练：`train_yolo.py` 1 epoch 跑通（loss 39.6→下降趋势正确），EMA/调度器/checkpoint（best/latest/final）落盘；
   - 推理：`infer_yolo.py` 生成 8 个提交 txt + zip，格式 `[cls cx cy w h conf]` 正确。
4. **正式训练前待办**：恢复 GPU（开卡或换实例）后按 23.3 步骤 7 依次执行 RGB 对照 → fusion 主路径 → 提交。

---

## 二十四、2026-08-28 实验突破：YOLO 方案首次出分，验证集 mAP@50-95 ≈ 0.30（v0.8.1）

### 24.1 突破概述

GPU 恢复后按第二十三节方案执行，YOLO11 RGB-only 对照（冻结 backbone，60 epoch）在 fold 1 验证集（400 张）上达到 **mAP@50-95 = 0.2971、mAP@50 = 0.5320**，较 M3F-DETR 一个月的最高成绩（≈0.005）提升约 60 倍，方向切换被实验证实有效。叠加水平翻转 TTA 后进一步到 **0.3231 / 0.5695**。当前已有可提交、大概率高于官方基线的结果。

### 24.2 实验记录（fold 1 验证集，RTX 5090）

| 实验 | 配置要点 | 最佳 mAP@50-95 | mAP@50 | 备注 |
|------|----------|---------------|--------|------|
| M3F-DETR（历史） | 自研 Swin+CMFA+DINO，1 个月 | ≈0.005 | ≈0.02 | 已废弃 |
| RGB v1 | yolo11n，冻结 backbone[0..9]，60ep，640×384 | 0.2971 | 0.5320 | 首次出分 |
| RGB v1 + TTA | 原图+水平翻转，类别内 NMS 融合 | 0.3231 | 0.5695 | +0.026 |
| Fusion v1 | 5ch（RGB+IR+Depth），冻结 backbone，80ep | 0.1782 | 0.3306 | 未跑赢 RGB |
| RGB v2 | 解冻 backbone 全量微调，100ep | 0.3104 | 0.5565 | best.pth（第 60 轮） |
| **RGB v2 + TTA** | 解冻 backbone + 水平翻转 TTA | **0.3234** | 0.5698 | 正式提交模型 |

RGB v1 每类 AP50-95（修复统计后）：seat 0.52、uav 0.40、light 0.39、car 0.37、animal 0.33。类别无塌缩，uav 等小目标已有可用 AP。置信度阈值扫描显示 `conf=0.001` 最优（0.2971），提交参数保持低阈值以保留完整排序。

### 24.3 关键问题根因与修复

#### 24.3.1 EMA 滞后导致 best.pth 选错（本轮最隐蔽的问题）

- **现象**：训练日志中验证 mAP 长期停在 0.0004~0.02，一度让人误以为模型没学出来；手工评估 `latest.pth`（原始模型）却发现 mAP 已达 0.27。
- **根因**：验证默认使用 EMA 模型，而 `ema_decay=0.9999` 的有效视窗约 10000 步；每 epoch 仅约 100 步时，训练 20 epoch 后 EMA 仍以初始随机权重为主，其置信度几乎全为 0，mAP 自然 ≈0。
- **修复**：
  1. `tools/train_yolo.py`：验证时用**原始模型**选择 best，EMA 仅作参考输出（验证日志同时打印 `--- Validation (EMA) ---`）；
  2. 配置 `ema_decay` 从 0.9999 调整为 0.999（视窗约 1000 步，EMA 在 10 epoch 内追上）。
- **遗留**：RGB v1 的 best.pth 由旧代码在 EMA 上选择，不可用；该模型最终取 `final.pth` 的原始权重。fusion/RGB-v2 起 best.pth 均有效。

#### 24.3.2 compute_map 每类 AP 恒为 0

- **根因**：pycocotools 的 `COCOeval.stats` 是空列表，只有调用 `summarize()` 后才会填充；旧 per-class 循环只 `evaluate()+accumulate()` 就读 `stats[0]`，抛 IndexError 被 `except` 静默吞掉，导致全部类别 AP 显示 0。
- **修复**：`engine/evaluator.py` per-class 循环中静默调用 `summarize()`（`contextlib.redirect_stdout`），随后读取 stats。
- **影响**：每类 AP 恢复可用，是后续类别不平衡诊断的基础。

#### 24.3.3 深度图格式与文档不符

- 赛题文档描述 16bit PNG 毫米深度，实际下载数据为 8bit JPG 可视化深度（0~255，多为 0）。`_load_depth` 现按 max>255 分支处理 16bit、否则按 1~255 线性映射，兼容两种格式。

#### 24.3.4 尺寸语义 bug

- `_resize` 将 `(H, W)` 解包为 `(w, h)`，输出被旋转式缩放；已修正为 `h, w = self.size`，并复验输出 `(5, 384, 640)`。

### 24.4 当前问题分析

1. **早期融合（fusion）未跑赢 RGB**：80 epoch 后 fusion 0.178 vs RGB 0.297。假设：
   - 1600 张训练数据下，均值初始化的 IR/Depth 通道引入的特征偏移需要更多数据/训练量才能被模型消化；
   - 当前增强过弱（仅 resize+翻转+RGB 光度），模型对融合通道的适应性不足；
   - 该数据集 RGB 本身已含大部分可判别信息，IR/Depth 对分数的边际贡献有限（需 per-class 与困难样本分析验证）。
2. **RGB 进入 ~0.30 平台期**：冻结 backbone（v1）与解冻 backbone（v2）均停在 0.29~0.31，说明当前瓶颈在数据增强/分辨率/类别不平衡，而非可训练参数量。
3. **EMA 与原始模型仍有差距**：0.999 衰减下 EMA 约 10 epoch 才追上；可进一步降到 0.99 或在最终推理时明确选择原始模型。
4. **低频类别 AP 低**：boat/ball/garbage can/tricycle 样本少，需要 per-class 统计 + 重采样。

### 24.5 后续方向（按优先级）

1. **尽快提交**：RGB-v2 训练完成后评估，取 v2/v1 中更优的原始权重 + `--tta` 生成正式提交包（`tools/infer_yolo.py` 已支持）。
2. **增强升级**：实现 mosaic / mixup / scale jitter / HSV（对齐 ultralytics 原生增强），预期突破 0.30 平台。
3. **分辨率实验**：640×384 → 800×512 / 1280×768，验证小目标（uav）与整体 AP 提升。
4. **类别不平衡**：按 GT 频次重采样训练集，或对低频类加权。
5. **融合改进（方案 B）**：双分支（RGB 主分支 + IR/Depth 轻量分支）在 P3-P5 做交叉注意力融合，替代简单早期融合；作为技术报告"创新点"。
6. **后处理**：conf/NMS 网格扫描（复用 sweep 思路）、TTA 已合入。

### 24.6 交接更新

- 当前正式提交：**RGB-v2 best.pth + TTA = 0.3234**（验证集口径），提交包 `submission_yolo_final.zip`（1000 个 txt，conf=0.001，NMS=0.6）。RGB v1+TTA 为 0.3231，与 v2 基本持平。
- 服务器已恢复 GPU（RTX 5090, 32GB），训练命令、checkpoint 路径、提交生成命令见 23.5/23.3 与 CHANGELOG v0.8.1。
- 本地仓库需 `git pull` 同步 v0.8.1 提交；远程 HEAD 推进至包含 v0.8.1 的提交。

---

*报告结束。*
